## 0. Working branch

- [x] 0.1 Isolated workspace created 2026-09-08: a **dedicated git worktree** at `.claude/worktrees/arabize-frontend-rtl`, branch `worktree-arabize-frontend-rtl`, branched from `origin/main` (verified level with local `main` — 0 commits of drift, so nothing was lost). A worktree rather than a plain branch because the owner has an unrelated Python-environment chantier uncommitted in the main tree; a branch switch would have carried it along and mixed the two indexes.
      **Consequence, and it is load-bearing:** a worktree born from git has **none** of the local-only files — it arrived with **zero test files**, no `vitest.config.ts`/`vitest.setup.ts`, no `CLAUDE.md`, and no copy of this change. That is design D20's claim demonstrated, not argued: `npx vitest run` there would have exited 0 on "No test files found". The eight test files, the Vitest config and setup, `CLAUDE.md`, this change directory and the drafted `strings.ts` were copied in by hand, and `node_modules` cloned copy-on-write. **Anyone re-creating this workspace must repeat that step or the safety net is absent.**
      *The copy of this change directory in the main tree is now a stale snapshot; the worktree copy is authoritative.*

## 1. Naming decisions (settled)

- [x] 1.1 Application name: «القرآن بالقرآن», replacing "Quran RAG" everywhere in the interface
- [x] 1.2 `/lexical` is «تحليل اللسان»; `/qlisan` is «بطاقة الكلمة» and is **removed from the navigation** (route and endpoints stay)
- [x] 1.3 `/tahlil` is «التحليل النحوي» (definite), settled 2026-09-08 — the indefinite «تحليل نحوي» read as a fragment beside four definite or annexed entries, and the definite form also stops the D11 collision from being character-identical. The colliding section is renamed too; see task 7.11
- [x] 1.4 Landing copy is **rewritten** in Arabic in a scholarly register, using the terminology the app already displays unglossed
- [x] 1.5 D9 ordering settled 2026-09-08: **this change lands first**, then `restructure-app-architecture` (which is at 0/39 tasks, so nothing is committed on either side). The three consequences, all recorded in design D9: (a) task 6.7 **translates** `LexicalResult.tsx` — 3 strings and one `ml-auto` — accepting that the restructure deletes it later; (b) `ArabicText.tsx:12` keeps `lang="ar"` and the canonical Arabic renderer is **exempted** from the redundant-`dir` rule, so the two archived specs agree; (c) `arabic-ui-locale`'s URL requirement is **scoped** to "SHALL NOT change as part of Arabization", leaving the restructure's `/lexical` slug realignment legitimate
- [x] 1.6 The `similar` tab is «الآيات القريبة في المعنى», settled 2026-09-08. «النظائر» is rejected: it names the discipline «الوجوه والنظائر» (a *word's* senses across verses) and this codebase already uses «نظيرة» in exactly that sense two pages away (`analysis/word_analysis.py:51` `_nazair()`, `TahlilClaim.tsx:9`), while the tab runs a semantic phrase search whose own on-screen sentence is «ما هي الآيات القريبة في المعنى من …» (`verse-study/page.tsx:648`). «الآيات المتشابهة» is also rejected — it names المتشابه اللفظي, verbal similarity, which this tab does not do (design D21). If the three-tab strip proves too tight in the 9.2 walk, «التقارب في المعنى» is the pre-approved shorter form
- [x] 1.7 Settled by 1.3 — `/tahlil` is definite, and the section at `LisanResult.tsx:236` is renamed rather than left colliding (task 7.11)

## 2. Baseline inventory

- [ ] 2.1 Pin the sweep inventory as an **executable command**, not a number, and record the command plus its output in this change directory. Verified counts as of 2026-09-08: **36 real physical-utility occurrences over 16 files, on 32 lines**; **58 element-level `dir` attributes** (48 `rtl`, 10 `ltr`); **12 `e?.message ||` sites** plus **4 backend-`message` sites** of a different class (task 5.9c). design.md's former "93 over 25 files" was inflated ≈2.6× because `rounded-l` matches **`rounded-lg`** (61 hits) and `right-`/`left-` match the English comments "right-to-left"/"right-aligned" (5 hits); its "59" `dir` attributes and any "102/26" figure are likewise wrong. Run from `frontend/src`, and note the `-nE`-then-filter order — `grep -o` strips the line, so a prose filter applied after it has nothing to match:

      RE='(^|[^a-zA-Z0-9-])-?((sm|md|lg|xl|2xl|hover|focus|focus-visible|group-hover|disabled|first|last|even|odd):)*(m[lr]-[0-9a-z.[]+|p[lr]-[0-9a-z.[]+|(left|right)-[0-9a-z.[/]+|text-(left|right)|border-[lr]([^a-z-]|$)|border-[lr]-[0-9a-z]+|rounded-([lr]|[tb][lr])([^a-z-]|$)|rounded-([lr]|[tb][lr])-[0-9a-z]+|translate-x-[0-9a-z./[]+)'
      grep -rnE "$RE" . --include='*.tsx' --include='*.ts' --include='*.css' \
        | grep -viE 'right-to-left|left-to-right|right-aligned|left-aligned'

- [ ] 2.1b Annotate every hit from 2.1 with the direction its element **resolves to today** — LTR-resolving, or inside one of the 48 `dir="rtl"` subtrees. This column decides which of the two mappings in design D12 applies and is unrecoverable once the `dir` attributes are deleted. Record it in the change directory, not only in a terminal
- [x] 2.2 Baseline captured 2026-09-08 and kept in `baseline/` in this directory. No launcher was needed and none was started: the owner already had a full stack running (backend on `:8001`, pre-change frontend on `:3000`), so the *unmodified* build was available to photograph directly — a better reference than anything we could have built. A redundant backend started on `:8000` was stopped: embedded Qdrant takes an exclusive lock, and the second instance could not acquire it (`RuntimeError: Storage folder ... is already accessed by another instance`), exactly as CLAUDE.md warns.
      Files: `baseline/before-fassila-analysis.jpg`, `baseline/before-surah-1.jpg`, `baseline/before-verse-study-word.jpg`.
      What they establish: `/fassila` is already RTL with **Western** digits throughout (`111`, `93`, `83.8%`, axis `1 2 15 93`); its sūra stepper is **RTL-correct** — the rightmost chevron points right (السابقة) and the left one points left (التالية), the four glyphs task 7.8 must leave alone; on `/verse-study` the search button sits to the **right** of its input today, which is the placement the RTL requirement inverts; and the root bar renders «الجذر رحم» on the right with the counts on the left, confirming task 5.3b's third hand-reversed row by eye

- [ ] 2.2b Add the axis invariant as a **machine check** rather than an eyeball: a Vitest case rendering each chart against a fixed 114-sūra fixture, asserting `x(sūra 1) < x(sūra 114)` in the emitted SVG and that each chart's scroll wrapper carries `dir="ltr"`. jsdom needs no layout engine — SVG coordinates are attributes, not computed styles. This assertion survives future changes; a screenshot does not
- [x] 2.3 Baseline green in the worktree, 2026-09-08, **after** restoring the git-excluded test files (see 0.1 — this task's original caveat turned out to be exactly what happened):
      · `npx vitest run` → **8 files, 77 tests, 0 failures** (2.15 s)
      · `npx tsc --noEmit` → clean, including the drafted `strings.ts`
      · `npx next build` → clean, **10 routes** emitted (`/`, `/chat`, `/fassila`, `/lexical`, `/qlisan`, `/tahlil`, `/verse-study`, `/surah/[number]`, `/verse/[surah]/[ayah]`, plus `/_not-found`) — which independently confirms the **nine** route pages of task 9.2 against the "eight routes" originally recorded
- [x] 2.4 Digit baseline captured 2026-09-08 — and it **closed** design D22 rather than feeding it. Measured in Chrome against Amiri v30 as this project serves it: `0123456789` renders pixel-identically (340.48 px) under `lang="en"`, under `lang="ar"`, and with `locl` forced on **or** off; real Arabic-Indic codepoints render as visibly different glyphs. On the live page the āya badge `﴿1﴾` at `verse-study/page.tsx:228` — Amiri, inside `lang="ar"`, without `.western-digits`, the exact configuration the hypothesis said must flip — shows a **Latin** digit. So `lang="ar"` on `<html>` does not reshape digits, and `.western-digits` is a no-op today.
      *Method note:* the substitution acts on glyphs, not characters — the DOM holds `255` in ASCII whatever the screen shows — so no assertion on rendered text could have settled this. It had to be looked at

## 3. Shell: direction, fonts, navigation

- [x] 3.1 Fonts vendored 2026-09-08 — **388 KB over 10 woff2 files** under `src/app/fonts/`: Amiri 400/700 and IBM Plex Sans Arabic 400/500/600, each in its `arabic` and `latin` subsets. `latin` (U+0000-00FF) is vendored because it carries the ASCII digits the numeral policy keeps Western *and* the accented characters of the French translation field; `latin-ext` and the Cyrillic subsets are not. Both OFL-1.1 licences ship beside the files, plus a `README.md` recording provenance and the reasoning. The three runtime `<link>` tags are gone from `layout.tsx`, and the now-empty `<head>` with them.
      **Declared with hand-written `@font-face`, not `next/font/local`** — see the amendment in design D6: `next/font/local` takes one file per weight and cannot express `unicode-range`, so it cannot serve a per-subset font without either dropping Latin (breaking the numeral policy) or emitting a dead duplicate rule. The files sit under `src/app/` rather than `public/` because `frontend/Dockerfile` copies `.next/static` and **not** `public/`
- [x] 3.1b Offline guarantee established **by construction plus artefact evidence**, 2026-09-08. After `rm -rf .next && npx next build`: `.next/static/media/` holds all **10** woff2 files (content-hashed), there is **no real `next/font` import** anywhere in the source, and **no pre-rendered HTML references Google Fonts**. The only `fonts.googleapis.com` strings left in the build output are constants inside Next's own framework runtime — its font-optimization code that detects such `<link>` tags, and its preconnect table — not a request this application makes.
      *Not yet done, and it is one command for the owner:* a literal network-off run. The build can no longer fetch a font because nothing asks it to, but turning the network off and repeating `rm -rf frontend/.next && npx next build` is the only way to observe it rather than infer it
- [x] 3.2 `tailwind.config.ts`: `ui: ["IBM Plex Sans Arabic", "Segoe UI", "system-ui", "sans-serif"]` gives the explicit `font-ui` handle, and **`sans` is overridden to the same stack** so Tailwind's preflight makes it the document default with no rule of our own. Verified safe: `font-sans` appears **nowhere** in the source, so the override changes no existing class. `font-arabic` stays bound to Amiri and its 20 call sites are untouched. The face is IBM Plex Sans Arabic and **not** "or Noto Kufi Arabic": Noto splits Latin into a separate family, so under it every Western digit, every `2:255` and every `dir="ltr"` detail line would fall through to a system fallback — on exactly the surfaces the numeral policy exists to stabilise
- [ ] 3.3 Set `<html lang="ar" dir="rtl">` and Arabic `<title>` / description metadata in `app/layout.tsx`. **Commit this alone** — it is the single highest-risk edit in the change (design Migration Plan step 2)
- [x] 3.4 The UI face is the document default through the `sans` override of 3.2 (Tailwind's preflight applies it to `html`), so `globals.css` needs no font rule on `body`. `.arabic-text` keeps `"Amiri", "Scheherazade New", serif` and its `line-height: 2.2` verbatim; `.western-digits` is untouched here — inverting its opt-out belongs with `lang="ar"` in task 3.4b, which is a different commit
- [x] 3.4b **Dropped 2026-09-08, by measurement — resolved, not pending.** This task would have inverted the `locl` opt-out into an opt-in across the app to protect the numeral policy from `lang="ar"`. Task 2.4 established that the substitution does not fire in this Amiri build at all, so the task defends against nothing. `.western-digits` stays in place — it costs nothing, it documents the intent, and it is the right shield if a future font revision implements the substitution — but it is not what keeps analytical numbers Western. See design D22
- [ ] 3.4c Reconcile the two āya-badge idioms: `surah/[number]/page.tsx:79` converts with a **local duplicate** of `toArabicDigits` (defined at `:11`) while `verse-study/page.tsx:228` relies on the font's `locl`. Delete the duplicate, import `@/lib/arabicDigits`, and make the conversion explicit at both sites so no rendering depends on a font feature
- [ ] 3.5 Change the content offset from `md:ml-64` to `md:ms-64` in `app/layout.tsx`
- [ ] 3.6 Move the `Navbar` sidebar to the right: `left-0` → `start-0`, `border-r` → `border-e`
- [ ] 3.6b Reduce the `Navbar` link list to five entries — «محاورة القرآن»، «دراسة الآية»، «الفواصل»، «تحليل اللسان»، «التحليل النحوي» — dropping the `/qlisan` entry (`Navbar.tsx:21`, the only `ScanText` consumer) and the now-unused icon import
- [ ] 3.6c Replace the "Quran RAG" brand in both the sidebar header (`:79`) and the mobile top bar (`:52`) with «القرآن بالقرآن»
- [ ] 3.6d Flip the active nav item's gradient at `Navbar.tsx:102`: `bg-gradient-to-r` is a physical `linear-gradient(to right, …)` and never follows `dir`, so after the side flip the pill's dark end sits under the icon and label. Use `bg-gradient-to-l` so `from-brand` stays at the row's leading (right) edge
- [ ] 3.7 Flip the mobile drawer's hidden transform from `-translate-x-full` to `translate-x-full` (transforms are physical — no logical utility does this; design D3)
- [ ] 3.8 Verify by hand: sidebar right on md+, drawer entering from the right, backdrop and close still working, content not sitting under the sidebar
- [ ] 3.8b Walk all **nine** routes at desktop and mobile widths immediately after 3.3, before group 4 begins. The P1 intermediate state is broken, not merely English: `ChatInterface.tsx:353` mis-stacks the whole transcript (task 5.2), and long English prose renders with its terminal punctuation on the wrong side under an RTL paragraph. Migration step 1's stated purpose — isolating layout bugs from wording bugs — only holds if this walk actually happens

## 4. The Arabic dictionary

- [x] 4.1 Create `src/lib/strings.ts` as one typed `as const` object grouped by page/component
- [ ] 4.1b Every label map SHALL be keyed by a **closed union**, never `Record<string, string>`, and SHALL NOT fall back to a machine identifier. Close the three open maps: `tahlil/page.tsx:18-24` `BLOCK_EN` (accessed at `:384` as `BLOCK_EN[id] ?? id`, so a missing key renders the raw Latin key `tarkib`), `MadarAslCard.tsx:25-27` `SOURCE_DISPLAY` (renders `maqayis_openiti`) and `:28-30` `EDITION_DISPLAY` (renders `Harun_DarAlFikr`). `TahlilClaim.tsx:7` and `StatusBadge.tsx:10` already do this correctly — follow them
- [x] 4.2 Write the navigation and page-name entries, using the names settled in group 1
- [x] 4.3 Write the per-page entries: headings, captions, tab labels, buttons, placeholders, ARIA labels, empty and loading states
- [x] 4.4 Write the `errors` group. Expose `errors.forStatus(status, kind)` returning a **distinct** Arabic sentence per failure kind rather than one generic sentence per call site, so the actionable information the backend already sends is not demoted along with the English (design D17)
- [x] 4.5 Express every interpolated string as a function so no Arabic sentence is concatenated in JSX (design D5)
- [x] 4.5b Add `iso()` — an FSI…PDI wrapper (`⁨`…`⁩`) — to `strings.ts`, and apply it to every interpolated Latin or numeric token. A dictionary function returns a `string` and therefore *cannot* emit the `dir="ltr"` span the risk register promised; FSI/PDI is the only isolation available to a plain string, and it also works inside `title` and `aria-label`, where an element cannot go. Grep the finished file for any `${` not passed through `iso()` and justify each (design D15)
- [x] 4.5c Add `count(n, forms)` implementing العدد والمعدود for the UI subset: `n === 1` singular; `n === 2` dual (آيتان/آيتين، فاصلتان/فاصلتين، سورتان/سورتين، أصلان/أصلين); `3 ≤ n ≤ 10` plural (آيات، فواصل، سور، أصول); `n ≥ 11` singular accusative (آية، فاصلة، سورة، أصلًا). Route every count through it. Call sites: `verse-study/page.tsx:716`; `FassilaBars.tsx:33,50`; `FassilaDiversityLine.tsx:251`; `FassilaDistributionPie.tsx:187`; `FassilaAnalysisTab.tsx:109-111`; `MadarAslCard.tsx:17-22` — whose `n >= 4` branch is already wrong for `n ≥ 11` («١١ أصول» must be «١١ أصلًا») and is the repo's own precedent for the rule (design D18)
- [ ] 4.5d Rewrite `ChatInterface.tsx:31-38` `relativeTime()` to call the dictionary entries — the Arabic side (`chat.justNow` / `minutesAgo` / `hoursAgo` / `daysAgo`) is drafted; what remains is the component rewrite, which belongs to the group-5 commit for `/chat`
- [ ] 4.5e Mechanise the three decidable classes the review gate should not be carrying, so 4.6 is left with register and idiom only: (a) a Vitest case asserting `count()` at n = 1, 2, 5, 11 for each noun series; (b) an assertion that no `${` in an Arabic template escapes `iso()`; (c) the Latin-residue grep over `strings.ts` (the (d) pass of 7.10c), run here rather than at the end. A human cannot check these more reliably than an assertion can, and they drown the one thing only a human can judge

> **Drafted 2026-09-08, not wired.** `frontend/src/lib/strings.ts` exists, `npx tsc --noEmit` is clean, and no component imports it — grep confirms zero call sites, so a correction here costs one edit rather than a re-wiring. The 4.5e checks were run by hand on the draft and all pass: no Latin residue in any value; every interpolation of unknown script goes through `iso()` (this caught two real defects — `questionWord` and `questionPhrase` echoed the user's query into mirrored guillemets without isolation, now fixed); and `count()` yields آية / آيتين / 5 آيات / 11 آية across n = 1, 2, 5, 10, 11, 286. The review table is `strings-review.md` in this directory.

- [x] 4.6 **Arabic review gate — blocking core.** Reviewer: **the owner** (single signatory). Signed **2026-09-08**, whole file accepted as drafted, no corrections. Group 5 is unblocked.
      Scope: the ~25 strings whose reach is cross-page, where a late correction is most expensive — the application name, the five navigation entries, the three Verse Study tab labels, the landing copy, and the shared vocabulary of the `errors` group. Read from `strings-review.md` in this change directory (key · Arabic · original English), not from the TypeScript.
      Record the outcome as a checklist appended to that file — one line per key group, accepted / corrected — with every correction applied to `strings.ts` before sign-off. **Group 5 does not start until this line is `[x]`.**
- [x] 4.6b **Rolling review — per page, non-blocking.** Pre-signed 2026-09-08 with Part A, so no group-5 commit waits on a reading of strings already drafted. Task 4.6c remains the channel for anything the wiring adds or changes. The strings local to one page are reviewed when that page is wired, at its own commit in group 5. Same three-column format, appended to the same file. This covers the page headings and captions, buttons, placeholders, empty and loading states, plus the eight Arabic templates that stay in components — three of them `title`/`aria-label` text no visual pass can see: `FassilaDiversityLine.tsx:251`, `FassilaLine.tsx:178`, `FassilaDistributionPie.tsx:187`, `FassilaBars.tsx:33`, `SarfiRows.tsx:63`, `qlisan/page.tsx:293-294`, `TahlilClaim.tsx:89`, `MadarAslCard.tsx:17-22`
      *Why splitting is safe:* the vocabulary that could diverge between two pages — page names, navigation, tab labels — is precisely what the blocking core in 4.6 holds. What is left is local to one screen
- [ ] 4.6c Re-review gate: any Arabic string added or changed during groups 5-7 goes back through the rolling review before 9.1. Record the delta, not the whole file

## 5. Pages: strings and direction

- [ ] 5.1 `app/page.tsx` — rewrite the landing hero, the three feature cards and the closing note in Arabic, scholarly register (rewrite, not translation); drop the "Arabic, French, or English" claim (`:15`), noting that the **capability remains** (`generation/prompts.py:29-30` still answers in the question's language) — this is a change of emphasis, not of behaviour, which is exactly why the answer bubble still needs `dir="auto"` (task 5.2b); replace `translate-x-0.5` on the CTA arrow so it points along the reading direction
- [ ] 5.2 `app/chat/page.tsx` + `components/ChatInterface.tsx` — 17 strings; confirm the composer renders textarea-right / send-button-left under RTL; `ml-auto` on the feedback row (`:388`) → `ms-auto`; **`text-right` on the user bubble (`:353`) → `text-end`**, without which every bubble stacks on the right and the transcript stops reading as a dialogue from the moment 3.3 lands; `text-left` in the conversation switcher (`:309`) → `text-start`; `ml-1` on the inline spinner (`:363`) → `ms-1`
- [ ] 5.2b `ChatInterface.tsx` — `dir="auto"` on the message content (`:361`) and on the composer textarea (`:432`). The LLM answers in the question's language and the user may type Latin script; without isolation a French answer is laid out RTL with its terminal punctuation on the wrong side (design D19)
- [ ] 5.2c `ChatInterface.tsx:445` — the send button is icon-only with **no** `aria-label` at all. The locale spec cannot be met by translating one; it must be added («إرسال»)
- [ ] 5.3 `app/verse-study/page.tsx` — 17 strings and its 5 real physical utilities; tab labels per the verse-study delta spec, with the `similar` label decided by task 1.6
- [ ] 5.3b `app/verse-study/page.tsx:394-455` — a **third** hand-reversed row, outside D4's two: the root bar renders `totals → root` under `justify-between`, so an LTR document paints «الجذر» on the right exactly as its comment says. Restore logical DOM order (`root → totals`), update the comment, and change `items-end` at `:403` to `items-start` — in a column container `items-end` is a cross-axis (inline) alignment and does flip, while `flex-col-reverse` beside it is block-axis and does not (design D13)
- [ ] 5.3c `app/verse-study/page.tsx` — `dir="auto"` on the echoed-query spans at `:334-336` and `:649-651`; `dir="ltr"` on the two bare `({surah_number})` spans at `:199` and `:783`
- [ ] 5.4 `app/qlisan/page.tsx` — 9 strings; restore the verse picker to logical DOM order (select → number → button) and switch `justify-end` to `justify-start` (design D4). Still done in full even though the page leaves the navigation, so a direct URL does not reach a half-migrated page (design D11)
- [ ] 5.4b `app/qlisan/page.tsx:270` — a **fourth** faked-RTL row: the loaded-verse card header is DOM-ordered `2:255 → سورة` and flushed right by `justify-end`. Restore to `سورة → 2:255` and drop the `justify-end` (an RTL row is start-aligned already). The `dir="rtl"` at `:274` is what makes this row look correct today, so task 7.9 must not delete it before this task runs
- [ ] 5.4c The three `/ {maxAyah}` hints (`qlisan:169`, `tahlil:187`, `verse-study:936`) are a bare `/` beside a number with no `dir` and no `.western-digits`: under an RTL paragraph the slash resolves to the paragraph level and reorders to the other side of the digits. Replace the punctuation with an Arabic word from the dictionary («من {n}») or isolate the token in `dir="ltr"`, and add `.western-digits`. design.md's Risks bullet names this hazard; no task discharged it until now
- [ ] 5.5 `app/tahlil/page.tsx` — 11 strings; same verse-picker restoration as 5.4
- [ ] 5.6 `app/lexical/page.tsx` — 6 strings; add the missing `aria-label="الكلمة"` on the word input at `:126` (its only accessible name today is `placeholder="رحمة"`, a sample value)
- [ ] 5.7 `app/fassila/page.tsx` — drop the now-redundant container `dir="rtl"` at `:41`; keep the Arabic tab labels it already has. Note this attribute is the ancestor that makes every Fassila physical utility an RTL-context site under D12, so 2.1b must be recorded first
- [ ] 5.8 `app/surah/[number]/page.tsx` and `app/verse/[surah]/[ayah]/page.tsx` — 6 strings between them, including the Latin `Surah {n-1}` / `Surah {n+1}` pagination labels
- [ ] 5.9 Replace every `e?.message || "…"` — **12 sites**: `lexical:103`, `qlisan:73,103,121`, `verse/[surah]/[ayah]:28`, `surah/[number]:31`, `verse-study:281,593,635,857,891`, `ChatInterface:231` — with `{ text: S.errors.forStatus(…), detail: e?.message }`, rendering the detail as a subordinate line inside `dir="ltr" lang="en"` (design D10, D17). Preserve the distinctions `api.ts` already makes: `api.ts:127` "not found" and `:128` "lookup failed" both feed `verse-study:891` today and must not collapse into one sentence
- [ ] 5.9b Every `dir="ltr"` technical-detail line from 5.9 also carries `lang="en"` — the `dir` so it lays out left-to-right, the `lang` so a screen reader does not spell English out with an Arabic voice
- [ ] 5.9c The **4 remaining** `message ||` sites are a different class and 5.9 does not fix them: `qlisan:367`, `qlisan:407`, `LisanResult:47`, `LisanResult:252` render a **backend** `message` field as the primary sentence. Three already fall back to Arabic; `LisanResult:47` does not, and its backend value is English (design D16, task 6.3c)
- [ ] 5.10 Arabize the default conversation title (`lib/conversations.ts:89` and `:123` → `S.chat.newConversation`, «محادثة جديدة»). Titles are **persisted**, so map the exact legacy value `"New conversation"` on read in `loadStore()` (`:56-73`). Do **not** touch `STORAGE_KEY` (`:12`, `"quran-rag.chat.v1"`) or `version: 1`: `loadStore()` rejects an unknown version silently and `saveStore` swallows failures, so renaming the key to match the new brand deletes every saved conversation with no error on screen

## 6. Components: strings and direction

- [ ] 6.1 `HealthBanner.tsx` — the three status sentences through a `strings.ts` function, with three corrections beyond translation: (a) the unreachable message must **not** name `./local-dev/start.sh` (`:39`) — that path is git-excluded (`.gitignore:46`) and a cloner does not have it; name the committed `./scripts/run.sh` inside `iso()`, or no path at all; (b) the degraded list joins with `" و"` — space **before** the waw, none after, because و is a proclitic; `join(" و ")` is wrong Arabic; (c) each Latin brand name and its parentheses pass through `iso()` (design D15)
- [ ] 6.1b `lib/conversations.ts` — see 5.10. This file appears in no Impact list today and is the change's only currently-failing test file outside the six named in group 8
- [ ] 6.2 `VerseCard.tsx`, `VerseContextCard.tsx` — strings, and **add** the `dir="ltr"` islands the spec requires: three of five reference sites have none today — `VerseCard.tsx:31,36` (`{name} ({surah}):{ayah}`), `VerseCard.tsx:41,44` (`[id]`), `VerseContextCard.tsx:37`. Include the bracket or paren inside the island: `(` `)` `[` `]` are bidi-mirrored and reverse when their resolved level is RTL. `qlisan/page.tsx:271` and `tahlil/page.tsx:297` are the pattern to copy
- [ ] 6.2b `VerseCard.tsx` — flip the name preference at `:19` to `verse.surah_name_ar || verse.surah_name_en`: Arabic is the required field and Latin the optional one (`types.ts:6-7`), yet the code prefers the Latin transliteration on the most-rendered component in the product. Translate «الجزء» (`:54`) and «درجة المطابقة» (`:58`), and map `verse.period` through a closed-union record → «مكية» / «مدنية» instead of rendering the raw value
- [ ] 6.2c `VerseCard.tsx` — `dir="auto" lang="fr"` / `dir="auto" lang="en"` on the translation paragraphs (`:68`, `:71`), which are bare `<p>` today and inherit RTL after 3.3 (design D19)
- [ ] 6.3 `LisanResult.tsx`, `MadarAslCard.tsx`, `SarfiRows.tsx`, `FicheRow.tsx`, `LevelCard.tsx` — strings and utilities. `SarfiRows`, `FicheRow` and `StatusBadge` are already RTL-native and need no directional edit; `SarfiRows.tsx:134` documents the rule («`items-start` = right edge in RTL») and is the reference for D12
- [ ] 6.3b Remove `LevelCard`'s `titleEn` prop (declared `:41`, rendered `:69` as an uppercase Latin gloss inside `dir="ltr"`). An English gloss beside every Arabic level heading is decoration in the wrong script, not an exempt identifier, and its `dir="ltr"` wrapper makes tasks 7.10 and 7.10b both pass it. Ripples: `qlisan/page.tsx:322,330,349-362,386,395,405,417`; `tahlil/page.tsx:384` and the then-dead `BLOCK_EN` map; the hand-rolled twin at `LisanResult.tsx:237-239` («Grammatical»); the comment at `LevelCard.tsx:58-60`, which documents the English label as intentional; and six `titleEn` usages in `LevelCard.test.tsx` (task 8.2b)
- [ ] 6.3c `lisan/lisan_service.py` — the one sanctioned backend edit (design D16). Replace the English `message` at `:178-181` with the Arabic sentence its sibling already uses verbatim (`madar/madar_service.py:179-181`: «تعذّر إيجاد جذر عربي للكلمة «…». قد تكون اسمَ علمٍ أو كلمةً خارج المعجم القرآني.») and translate `SOURCES` at `:41-45`; then drop the English fallback at `LisanResult.tsx:47`. Prose only — no schema, endpoint, retrieval or behaviour change
- [ ] 6.4 `TahlilClaim.tsx`, `StatusBadge.tsx`, `ScrollToTop.tsx`. `ScrollToTop.tsx:29` `right-6` → `end-6` is correct under the LTR mapping: the FAB must end up bottom-left, since the sidebar now occupies the right 256 px
- [ ] 6.4b Translate the six English accessible names: `qlisan/page.tsx:166`, `tahlil/page.tsx:184` and `:194`, `verse-study/page.tsx:933` («رقم الآية» / «السورة»), `ChatInterface.tsx:320` («حذف المحادثة»), `ChatInterface.tsx:396` («مفيد» / «غير مفيد»)
- [ ] 6.4c Add the three **missing** accessible names — the sūra selects at `qlisan/page.tsx:179` and `verse-study/page.tsx:919` (`aria-label="السورة"`, matching `tahlil/page.tsx:194`), and the word input covered by 5.6. A control with no accessible name satisfies "its name is Arabic" vacuously
- [ ] 6.5b `FassilaBars.tsx` — fix a bidi defect **already visible in production**, measured 2026-09-08: `:50` renders `{count} آية · {pct}%` and `/fassila` displays `%83.8`, the percent sign on the wrong side, on four rows. Cause (design D15): bidi rule W2 reclassifies the number as an *Arabic* number because «آية» precedes it, which stops rule W5 from attaching the `%`; the orphaned terminator then resolves right-to-left and lands left of the digits. Wrap the number and its `%` in a `dir="ltr"` span — measured to render `83.8%` correctly — and pass the same token through `iso()` in the `title` at `:33`, where no element can go. Record it as **pre-existing**, so task 9.3 does not attribute it to this change
- [ ] 6.5 `FassilaAnalysisTab.tsx`, `FassilaComparisonTab.tsx`, `FassilaTile.tsx`, `FassilaBars.tsx` — strings and utilities. All four are RTL-context sites under D12 (they render inside `fassila/page.tsx:41`'s `dir="rtl"`), so their `text-right` / `text-left` / `mr-2` take the **mirror** mapping
- [ ] 6.6 `FassilaLine.tsx`, `FassilaDiversityLine.tsx`, `FassilaDistributionPie.tsx` — strings only; do **not** alter the axis code or the physical utilities inside an **existing** `dir="ltr"` wrapper (design D7). Note that only `FassilaDiversityLine` has one today (`:142`, `:177`)
- [ ] 6.6b Add the missing LTR island the change's own spec mandates: `dir="ltr"` on `FassilaLine.tsx:89` (an `overflow-x-auto` around a `min-w-[560px]` SVG, with no `dir` at all), so the sequence chart opens at āya 1 on a narrow viewport instead of at the highest āya. This satisfies the **already-in-force** requirement `openspec/specs/fassila-surah-comparison/spec.md:589` "Chart scrolling is direction-independent", which `fassila/page.tsx:41` violates today. Record that `FassilaDistributionPie` needs none (no axis, no horizontal scroll) as a decision, not an omission
- [ ] 6.6c The two chart tooltips are **siblings** of the scroll wrapper (`FassilaLine.tsx:186-193`, `FassilaDiversityLine.tsx:282-291`) and their text is **Arabic** (`FassilaLine.tsx:178`, `FassilaDiversityLine.tsx:251`). They stay right-to-left — the spec's word "tooltips" in the LTR-islands list is amended so it cannot sweep them in (design D7). Add the missing `font-arabic` to `FassilaLine.tsx:189` so both tooltips share one face after 3.4, and clamp their inline `style={{ left: … }}` against the content area rather than the hardcoded `1200`: after the sidebar moves right, a `whitespace-nowrap` tooltip near a card's right edge renders under the `z-50` navigation
- [ ] 6.6d Decide, do not inherit, the placement of `FassilaDiversityLine.tsx:142`'s zoom controls. The `dir="ltr"` island there is not axis geometry (the axis starts at `:177`) — it pins flow-layout chrome to the physical right, which after the flip is the card's leading edge. Either move the island boundary so `justify-end` means the RTL trailing edge, or record the physical-right placement as intentional
- [ ] 6.6e **Keep `FassilaComparisonTab.tsx:139` right-to-left** — do not add an island. Its first column is «السورة», so an RTL scroll container opens on it; a `dir="ltr"` island would open the table on its last columns instead. The archived requirement is scoped to "a chart whose X axis reads left-to-right", which a table is not. Recorded as a decision so it is not re-read later as an oversight
- [ ] 6.7 `LexicalResult.tsx` — **translate** its 3 strings and convert its one `ml-auto` at `:23` (an LTR-context site, so `ms-auto`). Task 1.5 settled the ordering in favour of this change, so the component is still live when this lands even though the restructure deletes it afterwards
- [ ] 6.8 `ArabicText.tsx` — absent from every Impact list and task until now, yet it carries the `dir="rtl" lang="ar"` at `:12` that task 7.9 targets, and it is the container `VerseCard.tsx:63` and `VerseContextCard.tsx:59` pass `text-right` into (task 7.4). Keep `lang="ar"`; the `dir` decision is settled by task 1.5 (design D9)
- [ ] 6.9 `lib/api.ts` — absent from every Impact list until now. Leave all 23 English `new Error(…)` messages (`:37`…`:274`) intact: under D10 they become the subordinate `dir="ltr"` detail lines, which is the fourth exempt class the locale spec now names. The `"Quran RAG"` comment at `:1` is excluded from 9.2c by the comments-stay-English policy

## 7. Direction sweep

*Order matters: classification and conversion come **before** deletion. The `dir="rtl"` attributes are the sweep's own input data (design D12).*

- [ ] 7.1 Confirm the 2.1b annotation is complete and recorded before touching a utility
- [ ] 7.2 Convert the **LTR-context** sites (no `dir="rtl"` ancestor): `ml-`→`ms-`, `mr-`→`me-`, `pl-`→`ps-`, `pr-`→`pe-`, `left-`→`start-`, `right-`→`end-`, `text-left`→`text-start`, `text-right`→`text-end`, `border-l`→`border-s`, `border-r`→`border-e`, `rounded-l*`→`rounded-s*`, `rounded-r*`→`rounded-e*`
- [ ] 7.3 Convert the **RTL-context** sites with the **mirror** mapping — `mr-`→`ms-`, `ml-`→`me-`, `pr-`→`ps-`, `pl-`→`pe-`, `right-`→`start-`, `left-`→`end-`, `text-right`→`text-start`, `text-left`→`text-end`, `border-r`→`border-s`, `border-l`→`border-e` — because inside a `dir="rtl"` subtree the physical right already **is** inline-start. Confirmed members: the whole `fassila/page.tsx:41` subtree (`FassilaAnalysisTab.tsx:170,173,176`; `FassilaComparisonTab.tsx:146`; `FassilaDistributionPie.tsx:188`; `FassilaBars.tsx:49`; `FassilaTile.tsx:29`), `LisanResult.tsx:72,102,241,275,282`, `MadarAslCard.tsx:102`, `verse-study/page.tsx:194,714,772`. Commit 7.2 and 7.3 separately so a mis-classification reverts on its own
- [ ] 7.4 Handle the three **cross-boundary alignment** sites by hand, not by either table: `verse-study/page.tsx:219`, `VerseCard.tsx:63`, `VerseContextCard.tsx:59` each set `text-right` on an LTR-resolving ancestor that is inherited by a `direction: rtl` descendant (`ArabicText` / `.arabic-text`). `text-align` is inherited but resolved against **each element's own** direction, so `text-end` there would left-align every verse body. Move the alignment onto the RTL element as `text-start`, or drop it — an RTL block is start-aligned already
- [ ] 7.5 Audit the **direction-aware** utilities the physical grep cannot see, and re-derive each from intent (design D13): `justify-end` (`qlisan:140,270`, `tahlil:160`, `FassilaDiversityLine:142`), `items-end` (`verse-study:403`), `items-start` (`ChatInterface:282`, `SarfiRows:108,110,137`, `HealthBanner:53`, `VerseContextCard:47`, `FassilaDistributionPie:126`), `ml-auto`/`mr-auto` (`VerseCard:57`, `ChatInterface:388`, `LisanResult:72,102,241`, `LexicalResult:23`). For each, state the side it renders on today and the side it must render on after. Leave `ChatInterface.tsx:294`'s menu alone — it sets no inset, so its static position flips for free
- [ ] 7.6 Extend the sweep past the mapping tables to the properties that are physical and have **no** logical utility. Verified in the tree: `translate-x` (`Navbar:67,68`, `page.tsx:83`), `bg-gradient-to-r` (`Navbar:102`), and the three `fixed` chart tooltips positioned by inline `style={{ left: … }}` (`FassilaLine:190`, `FassilaDiversityLine:289`, `FassilaDistributionPie:242`) — none of which any class grep can find. Verified **zero** hits for `space-x-*`, `divide-x-*`, `origin-left/right`, `float-*`, `inset-x-*`, `scroll-m/p-l/r`, horizontal `box-shadow` offsets, `background-position`, `clip-path`, `text-indent` and arbitrary-value directional classes. Record the empty results too, so 7.10 re-runs an identical query
- [ ] 7.7 Mirror the eight semantically-directional icon glyphs — icons are SVG paths and do not mirror with `dir` (design D14): `page.tsx:83` (`ArrowRight`→`ArrowLeft`, `translate-x-0.5`→`-translate-x-0.5`), `surah/[number]/page.tsx:92,102` (swap), `verse/[surah]/[ayah]/page.tsx:95,105` (swap), `VerseContextCard.tsx:76`, `qlisan/page.tsx:437`, and `ChatInterface.tsx:453` (`Send` has no mirrored counterpart in lucide — flip it with `-scale-x-100`)
- [ ] 7.8 Confirm the four already-RTL-correct glyphs are **untouched**: `FassilaAnalysisTab.tsx:62` (`ChevronRight`, `aria-label="السورة السابقة"`) and `:83` (`ChevronLeft`, «التالية»), `verse-study/page.tsx:207,530` (collapsed disclosures). Their Arabic labels are the proof they were authored for RTL. Every `ChevronDown` and every `rotate-180` on one is direction-neutral and also stays
- [ ] 7.8b Replace or pin the three literal `←` (U+2190) glyphs, whose codepoint is `Bidi_Mirrored`, so their rendered direction inside an RTL run is engine-dependent: `LisanResult.tsx:150` (the «القراءة التتابعية» chain separator, which must point along the flex row) and `verse/[surah]/[ayah]/page.tsx:49` (a "back" affordance, which must point right). Use an SVG icon so the direction is a decision, not a renderer behaviour. Leave `FassilaAnalysisTab.tsx:152-153` — those are "becomes" arrows inside Arabic prose — but record that they were checked
- [ ] 7.9 **Now** delete every element-level `dir="rtl"` that only restates the document direction (48 counted in 2.1), except the ones tasks 5.4b, 6.6c and 6.8 keep. Commit alone
- [ ] 7.10 Re-run the 2.1 and 7.6 commands verbatim and confirm every remaining physical utility and every remaining `dir` attribute sits inside a declared LTR island — **and** diff each converted site against its 2.1b annotation. A zero-hit grep cannot distinguish a correct logical utility from an inverted one; it is a coverage check, never a correctness check
- [ ] 7.10b Resolve the 6.6 / 7.10 conflict per site rather than by exception: `FassilaDistributionPie.tsx:188` (`text-right` on the legend button) and `FassilaBars.tsx:49` (`text-left` on the stats column) are flow-layout chrome, not axis geometry. Convert them under the D12 mirror mapping and record that the pie legend and the bar rows sit **outside** the chart's LTR island (design D7)
- [ ] 7.10c Latin-text audit as four scoped passes over `frontend/src`, excluding `*.test.*`, recording each output in the change. A single naive grep is unusable — `[A-Za-z]{2,} [A-Za-z]{2,}` returns ~2000 lines and a quoted-string pass ~950, dominated by `className`, module specifiers and `pageCache` keys. (a) JSX text nodes: `grep -rnoE '>[A-Za-z][A-Za-z ,.?!-]{2,}<' --include='*.tsx'` (10 hits today). (b) user-visible attributes: `grep -rnoE '(placeholder|aria-label|title|alt)="[^"]*[A-Za-z]{2,}'` (7 hits). (c) every `useState<string>` / `let …: string` / `setError(` / `setMessage(` initialiser holding a Latin sentence — `HealthBanner.tsx:39-47` builds its English into a local variable and is reachable **only** this way. (d) `strings.ts` itself, for Latin residue. Permitted residue is exactly the **four** classes the locale spec names. A `dir="ltr"` wrapper does **not** make a string exempt: `LisanResult.tsx:237-239` («Grammatical») and `LevelCard.tsx:69` (`titleEn`) are both islanded and both violations
- [ ] 7.11 Resolve the D11 collision as settled in 1.3/1.7 — both sides move, so the fix is a rename, not a note. The page is «التحليل النحوي» (task 3.6b), and the collapsible section at `LisanResult.tsx:236` becomes **«الصرف والإعراب»**, which is what it actually renders: `SarfiRows` is morphology (صرف) plus a case-marker hint derived from the نحوي level, so its old label was loose independently of this change. Also update `LisanResult.test.tsx:15`, which hard-codes `const TITLE = "تحليل نحوي"`, and route the new label through `strings.ts` so 4.6 reviews it

## 8. Tests

- [ ] 8.1 Update `verse-study/page.test.tsx`: (a) the two exact-match tab queries at `:176` and `:263` → `S.verseStudy.tabs.*`; (b) the `/Open full Sourate page/` link query at `:219` → the key `VerseContextCard.tsx:75` now renders; (c) **rewrite** the error test at `:281-288` — it mocks `new Error("Verse not found")` and asserts that same text is on screen, so under D10 the detail line keeps it green **whether or not the Arabic sentence was ever wired**. Assert instead that `S.errors.*` is present AND that the element carrying `e.message` declares `dir="ltr"`
- [ ] 8.2 Update `lexical/page.test.tsx`, `LisanResult.test.tsx`, `LevelCard.test.tsx`, `TahlilClaim.test.tsx`, `MadarAslCard.test.tsx` where they assert on chrome text, importing `S.*` rather than retyping Arabic (design D8). Scope note: these four component suites assert almost entirely on Arabic domain vocabulary already exempt under D5(b), so 8.2 is smaller than it looks and 8.1 larger
- [ ] 8.2b `LevelCard.test.tsx` **supplies** `titleEn` as a required prop at `:11-12,25,37-38,69,89,101-102`; removing the prop (6.3b) breaks compilation, not just assertions. Drop it from all six
- [ ] 8.2c Update `src/lib/conversations.test.ts:130,132`, which assert `deriveTitle(...) === "New conversation"`. This file is named in no task today and would fail task 8.5 in a file no one owns
- [ ] 8.3 Assert the shell contract **without rendering into the ambient document**: `RootLayout` returns `<html><body>`, which Testing Library mounts as a child of `document.body`, so an assertion on `document.documentElement` tests jsdom's own document and always fails. Use `renderToStaticMarkup(<RootLayout>…</RootLayout>)`, or `render(...)` plus `container.querySelector("html")`, and assert `lang="ar"` and `dir="rtl"` on that element. Requires `vi.mock("next/navigation", …)` — `Navbar.tsx:33-36` dereferences `usePathname()` and throws without a router — and `vi.mock("next/font/local", …)` returning `{ className: "", variable: "" }`, because `node_modules/next/font/local/index.js` is a **0-byte** webpack-loader stub and is `undefined` under Vitest. What this proves: the layout module emits the attributes. Nothing more
- [ ] 8.4 Assert the composer and verse-picker contract as **DOM order only** — `input.compareDocumentPosition(button) & Node.DOCUMENT_POSITION_FOLLOWING` — for the chat composer, the word search, the phrase search and both verse pickers. Do **not** assert geometry: jsdom has no layout engine, `getBoundingClientRect()` returns zeros, and Vitest's `test.css` defaults to false so Tailwind is never loaded — any left/right comparison passes vacuously. Visual placement is task 9.4's job, in a browser
- [ ] 8.5 `npx vitest run` green
- [ ] 8.6 Typecheck the test files too. `tsconfig.json` excludes `**/*.test.ts(x)` so `next build` skips them, and Vitest transpiles with esbuild without typechecking — a mistyped `S.*` key in a test resolves to `undefined` and is caught by nothing. Add a local-only `tsconfig.test.json` extending the base with the tests included, run `npx tsc --noEmit -p tsconfig.test.json` alongside 8.5, and add it to `.gitignore` beside the other local-only test files
- [ ] 8.7 Record inside this change directory the evidence a clone cannot reproduce, because every test file is git-excluded and the CI workflow is local-only: the 2.1 / 7.6 commands and their output, the 8.5 summary, the 9.1 build output, and the 2.2 / 2.2b chart baseline. Every assertion added in 2.2b, 8.3 and 8.4 must also exist as a scenario in `specs/`, which **is** committed (design D20)

## 9. Verification

- [ ] 9.1 `next build` clean, no TypeScript errors from the typed dictionary
- [ ] 9.2 Walk all **nine** routes (`/`, `/chat`, `/verse-study`, `/fassila`, `/lexical`, `/tahlil`, `/qlisan`, `/surah/[number]`, `/verse/[surah]/[ayah]`) at desktop and mobile widths: navigation right, no English chrome, no horizontally clipped content
- [ ] 9.2b Confirm the navigation shows exactly five entries with no `/qlisan` link, and that `/qlisan` opened by direct URL is fully Arabic and RTL
- [ ] 9.2c Scoped brand check — three commands, all must return zero, because a bare grep cannot pass without breaking the comments-stay-English policy:

      git grep -n "Quran RAG" -- frontend/src ':!*.test.*' ':!frontend/src/lib/api.ts'
      rm -rf frontend/.next && (cd frontend && npx next build) && grep -rl "Quran RAG" frontend/.next/static frontend/.next/server

  `api.ts:1` is a code comment and stays English. The 11 non-frontend hits (`scripts/`, `api/main.py`, `README.md`, `indexing/`, `ingestion/`) are **out of scope**: this is an interface rename, not a repo rename. Wipe `.next` first — its cache holds pre-change artifacts
- [ ] 9.2d Digit pass, now a **confirmation rather than a hunt** (task 2.4 removed the risk): check that the analytical numbers are still Western and that the āya badges are Arabic-Indic **at both** of the sites reconciled in 3.4c. Still done by eye, because the substitution would act on glyphs and not on characters
- [ ] 9.2e Load a **pre-change** `localStorage` payload (a `version: 1` store whose conversation carries `title: "New conversation"`), open `/chat`, and confirm the switcher shows no English default and that a new message re-derives the title
- [ ] 9.3 Compare the three Fassila charts against the 2.2 captures **and** run the 2.2b assertions — sūra 1 still leftmost, sūra 114 still rightmost, tooltips still readable. At mobile width additionally check the **scroll origin**: the 2.2 captures cannot certify it, because `FassilaLine` opens at the wrong end *before* this change too (task 6.6b). Also eyeball what neither the captures nor any test covers: `FassilaTile` hint spacing, the `FassilaBars` digit column, and the `FassilaAnalysisTab` / `FassilaComparisonTab` table headers — all four are 7.3 mirror-mapping sites
- [ ] 9.4 Check every input row at **desktop** width: chat composer, word search, phrase search, both verse pickers — action button on the left of its input. At mobile width these rows wrap (`flex-wrap`), so the button drops to its own line and the placement rule does not bind; check DOM order there instead
- [ ] 9.4b Walk every prev/next control and confirm "previous" points toward the start (right) and "next" toward the end (left), and that the four RTL-correct glyphs of 7.8 are unchanged
- [ ] 9.4c Check native control chrome, which no logical utility governs: on the three `<input type="number">` āya boxes (`qlisan:161`, `tahlil:179`, `verse-study:926`) and the four `<select>`s (`qlisan:176`, `tahlil:195`, `verse-study:916`, `FassilaAnalysisTab:66`), confirm the UA-drawn spinners and disclosure arrow moved to the left without colliding with the text, and that `verse-study:926` — the only āya box without `text-center` — matches its siblings
- [ ] 9.5 Force a backend-down state and confirm the failure path is Arabic, with any English exception text confined to a subordinate `dir="ltr" lang="en"` line. Then force a 422 by submitting Latin script to «تحليل اللسان» and confirm the Arabic sentence still says what to fix (design D17)
- [ ] 9.6 Keyboard pass: tab order follows the visual right-to-left reading order, drawer focus behaves. Neither tab strip is an ARIA `tablist` — they are plain buttons, so traversal is Tab through DOM order and no arrow-key handler needs mirroring
- [ ] 9.7 Because group 8 cannot cover the mirroring axis at all, make the manual pass explicit and per-site: for every site listed in 7.3, 7.4, 7.5, 7.7 and 5.3b, record the side it rendered on before and after. A screenshot diff is insufficient on its own — it cannot see a defect that pre-exists the change
