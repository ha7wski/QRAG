## 0. Working branch

- [x] 0.1 Isolated workspace created 2026-09-08: a **dedicated git worktree** at `.claude/worktrees/arabize-frontend-rtl`, branch `worktree-arabize-frontend-rtl`, branched from `origin/main` (verified level with local `main` — 0 commits of drift, so nothing was lost). A worktree rather than a plain branch because the owner has an unrelated Python-environment chantier uncommitted in the main tree; a branch switch would have carried it along and mixed the two indexes.
      **Consequence, and it is load-bearing:** a worktree born from git has **none** of the local-only files — it arrived with **zero test files**, no `vitest.config.ts`/`vitest.setup.ts`, no `CLAUDE.md`, and no copy of this change. That is design D20's claim demonstrated, not argued: `npx vitest run` there would have exited 0 on "No test files found". The eight test files, the Vitest config and setup, `CLAUDE.md`, this change directory and the drafted `strings.ts` were copied in by hand, and `node_modules` cloned copy-on-write. **Anyone re-creating this workspace must repeat that step or the safety net is absent.**
      *The copy of this change directory in the main tree is now a stale snapshot; the worktree copy is authoritative.*

## 1. Naming decisions (settled)

- [x] 1.1 Application name: «القرآن بالقرآن», replacing "Quran RAG" everywhere in the interface
- [x] 1.2 `/lexical` is «تحليل اللسان»; `/qlisan` is «بطاقة الكلمة» and is **removed from the navigation** (route and endpoints stay)
- [x] 1.3 `/tahlil` is «التحليل النحوي» (definite), settled 2026-09-08 — the indefinite «تحليل نحوي» read as a fragment beside four definite or annexed entries, and the definite form also stops the D11 collision from being character-identical. The colliding section is renamed too; see task 7.11
- [x] 1.4 Landing copy is **rewritten** in Arabic in a scholarly register, using the terminology the app already displays unglossed
- [x] 1.5 D9 ordering settled 2026-09-08: **this change lands first**, then `restructure-app-architecture` (which is at 0/39 tasks, so nothing is committed on either side). The three consequences, all recorded in design D9: (a) task 6.7 **translates** `LexicalResult.tsx` — 3 strings and one `ml-auto` — accepting that the restructure deletes it later; (b) `ArabicText.tsx:12` keeps `lang="ar"` and the canonical Arabic renderer is **exempted** from the redundant-`dir` rule, so the two archived specs agree; (c) `arabic-ui-locale`'s URL requirement is **scoped** to "SHALL NOT change as part of Arabization", leaving the restructure's `/lexical` slug realignment legitimate
- [x] 1.6 The `similar` tab is «الآيات القريبة في المعنى», settled 2026-09-08. «النظائر» is rejected: it names the discipline «الوجوه والنظائر» (a *word's* senses across verses) and this codebase already uses «نظيرة» in exactly that sense two pages away (`analysis/word_analysis.py:51` `_nazair()`, `TahlilClaim.tsx:9`), while the tab runs a semantic phrase search whose own on-screen sentence is «ما هي الآيات القريبة في المعنى من …» (`verse-study/page.tsx:648`). «الآيات المتشابهة» is also rejected — it names المتشابه اللفظي, verbal similarity, which this tab does not do (design D21). If the three-tab strip proves too tight in the 9.2 walk, «التقارب في المعنى» is the pre-approved shorter form
- [x] 1.7 Settled by 1.3 — `/tahlil` is definite, and the section at `LisanResult.tsx:236` is renamed rather than left colliding (task 7.11)

## 2. Baseline inventory

- [x] 2.1 Pinned as a command and recorded in `evidence.md` — and made reproducible in a way the task did not ask for but which is strictly better: `git grep <rev>` applies the same expression to `main` and to `HEAD` without checking either out, so the "after" number is not a claim about a working tree that no longer exists. **32 lines over 16 files** before (the 36 occurrences sit on 32 lines); **3 lines over 2 files** after.
      All three survivors are of one kind — **transforms**, which have no logical form at all (design D13), so their sign was flipped by hand and no grep can tell that from an oversight: `page.tsx:77`, `Navbar.tsx:78-79`. Also recorded: `dir` attributes went 48 rtl / 10 ltr → **2** rtl / 23 ltr / 8 auto, the two survivors being exactly the two design D9 exempts
- [x] 2.1b Superseded by the correction to D12, and recorded as such rather than performed. The annotation existed to choose between two mapping tables; measurement showed there is **one** table outside the LTR islands, because every element outside an island resolves RTL after the flip. What the pre-flip direction is still good for — judging *intent* on a site that used to resolve LTR — was needed at exactly two sites, both decided in the open and both recorded in D12 (the chat bubble, `ScrollToTop`).
      The measurement itself is in D12: `closest('[dir="rtl"]')` run on the live pages, which answers per element rather than by inferring JSX nesting from indentation. The one place the *original* two-table rule turned out to be right is auto margins on flex items — found later, in group 6, and recorded as task 7.12
- [x] 2.2 Baseline captured 2026-09-08 and kept in `baseline/` in this directory. No launcher was needed and none was started: the owner already had a full stack running (backend on `:8001`, pre-change frontend on `:3000`), so the *unmodified* build was available to photograph directly — a better reference than anything we could have built. A redundant backend started on `:8000` was stopped: embedded Qdrant takes an exclusive lock, and the second instance could not acquire it (`RuntimeError: Storage folder ... is already accessed by another instance`), exactly as CLAUDE.md warns.
      Files: `baseline/before-fassila-analysis.jpg`, `baseline/before-surah-1.jpg`, `baseline/before-verse-study-word.jpg`.
      What they establish: `/fassila` is already RTL with **Western** digits throughout (`111`, `93`, `83.8%`, axis `1 2 15 93`); its sūra stepper is **RTL-correct** — the rightmost chevron points right (السابقة) and the left one points left (التالية), the four glyphs task 7.8 must leave alone; on `/verse-study` the search button sits to the **right** of its input today, which is the placement the RTL requirement inverts; and the root bar renders «الجذر رحم» on the right with the counts on the left, confirming task 5.3b's third hand-reversed row by eye

- [x] 2.2b `src/components/chartAxis.test.tsx`, 5 assertions over a 120-āya and a 114-sūra fixture: each chart's plotted `cx` values are **monotonic** (not merely increasing end to end), each scroll wrapper declares `dir="ltr"`, and the zoom label keeps the island of its own that task 6.6d gave it. Free in jsdom, as predicted — SVG coordinates are attributes.
      One thing the plan did not foresee, and it is a trap worth recording: rendering **once at module scope** makes every case after the first assert on an empty tree, because Testing Library's global `cleanup` empties the container between tests — and the first case still passes, so the file looks healthy. Each case renders for itself. **Proved able to fail**: removing `FassilaLine`'s island fails the scroll-origin assertion
- [x] 2.3 Baseline green in the worktree, 2026-09-08, **after** restoring the git-excluded test files (see 0.1 — this task's original caveat turned out to be exactly what happened):
      · `npx vitest run` → **8 files, 77 tests, 0 failures** (2.15 s)
      · `npx tsc --noEmit` → clean, including the drafted `strings.ts`
      · `npx next build` → clean, **10 routes** emitted (`/`, `/chat`, `/fassila`, `/lexical`, `/qlisan`, `/tahlil`, `/verse-study`, `/surah/[number]`, `/verse/[surah]/[ayah]`, plus `/_not-found`) — which independently confirms the **nine** route pages of task 9.2 against the "eight routes" originally recorded
- [x] 2.4 Digit baseline captured 2026-09-08 — and it **closed** design D22 rather than feeding it. Measured in Chrome against Amiri v30 as this project serves it: `0123456789` renders pixel-identically (340.48 px) under `lang="en"`, under `lang="ar"`, and with `locl` forced on **or** off; real Arabic-Indic codepoints render as visibly different glyphs. On the live page the āya badge `﴿1﴾` at `verse-study/page.tsx:228` — Amiri, inside `lang="ar"`, without `.western-digits`, the exact configuration the hypothesis said must flip — shows a **Latin** digit. So `lang="ar"` on `<html>` does not reshape digits, and `.western-digits` is a no-op today.
      *Method note:* the substitution acts on glyphs, not characters — the DOM holds `255` in ASCII whatever the screen shows — so no assertion on rendered text could have settled this. It had to be looked at

## 3. Shell: direction, fonts, navigation

- [x] 3.1 Fonts vendored 2026-09-08 — **388 KB over 10 woff2 files** under `src/app/fonts/`: Amiri 400/700 and IBM Plex Sans Arabic 400/500/600, each in its `arabic` and `latin` subsets. `latin` (U+0000-00FF) is vendored because it carries the ASCII digits the numeral policy keeps Western *and* the accented characters of the French translation field; `latin-ext` and the Cyrillic subsets are not. Both OFL-1.1 licences ship beside the files, plus a `README.md` recording provenance and the reasoning. The three runtime `<link>` tags are gone from `layout.tsx`, and the now-empty `<head>` with them.
      **Declared with hand-written `@font-face`, not `next/font/local`** — see the amendment in design D6: `next/font/local` takes one file per weight and cannot express `unicode-range`, so it cannot serve a per-subset font without either dropping Latin (breaking the numeral policy) or emitting a dead duplicate rule. The files sit under `src/app/` rather than `public/` because `frontend/Dockerfile` copies `.next/static` and **not** `public/`
- [x] 3.1b Offline guarantee established **by construction plus artefact evidence**, 2026-09-08. After `rm -rf .next && npx next build`: `.next/static/media/` holds all **10** woff2 files (content-hashed), there is **no real `next/font` import** anywhere in the source, and **no pre-rendered HTML references Google Fonts**. The only `fonts.googleapis.com` strings left in the build output are constants inside Next's own framework runtime — its font-optimization code that detects such `<link>` tags, and its preconnect table — not a request this application makes.
      *Not yet done, and it is one command for the owner:* a literal network-off run. The build can no longer fetch a font because nothing asks it to, but turning the network off and repeating `rm -rf frontend/.next && npx next build` is the only way to observe it rather than infer it
- [x] 3.2 `tailwind.config.ts`: `ui: ["IBM Plex Sans Arabic", "Segoe UI", "system-ui", "sans-serif"]` gives the explicit `font-ui` handle, and **`sans` is overridden to the same stack** so Tailwind's preflight makes it the document default with no rule of our own. Verified safe: `font-sans` appears **nowhere** in the source, so the override changes no existing class. `font-arabic` stays bound to Amiri and its 20 call sites are untouched. The face is IBM Plex Sans Arabic and **not** "or Noto Kufi Arabic": Noto splits Latin into a separate family, so under it every Western digit, every `2:255` and every `dir="ltr"` detail line would fall through to a system fallback — on exactly the surfaces the numeral policy exists to stabilise
- [x] 3.3 Done 2026-09-08, **committed alone** as the Migration Plan requires. `<html lang="ar" dir="rtl">`, and the metadata now reads from the dictionary (`S.app.title` / `S.app.description`) rather than from literals, so `layout.tsx` is the first consumer of `strings.ts`. Verified served: `<html lang="ar" dir="rtl">` and `<title>القرآن بالقرآن</title>`
- [x] 3.4 The UI face is the document default through the `sans` override of 3.2 (Tailwind's preflight applies it to `html`), so `globals.css` needs no font rule on `body`. `.arabic-text` keeps `"Amiri", "Scheherazade New", serif` and its `line-height: 2.2` verbatim; `.western-digits` is untouched here — inverting its opt-out belongs with `lang="ar"` in task 3.4b, which is a different commit
- [x] 3.4b **Dropped 2026-09-08, by measurement — resolved, not pending.** This task would have inverted the `locl` opt-out into an opt-in across the app to protect the numeral policy from `lang="ar"`. Task 2.4 established that the substitution does not fire in this Amiri build at all, so the task defends against nothing. `.western-digits` stays in place — it costs nothing, it documents the intent, and it is the right shield if a future font revision implements the substitution — but it is not what keeps analytical numbers Western. See design D22
- [ ] 3.4c Reconcile the two āya-badge idioms: `surah/[number]/page.tsx:79` converts with a **local duplicate** of `toArabicDigits` (defined at `:11`) while `verse-study/page.tsx:228` relies on the font's `locl`. Delete the duplicate, import `@/lib/arabicDigits`, and make the conversion explicit at both sites so no rendering depends on a font feature
- [x] 3.5 `md:ml-64` → `md:ms-64`. Verified: main content occupies the left of the viewport and does not slide under the sidebar
- [x] 3.6 `left-0` → `start-0`, `border-r` → `border-e`. Verified by computed style, not by eye: `right: 0px` / `left: 1214px` (pinned to the right edge) and `border-left-width: 1px` / `border-right-width: 0px` (the border faces the content)
- [x] 3.6b Reduce the `Navbar` link list to five entries — «محاورة القرآن»، «دراسة الآية»، «الفواصل»، «تحليل اللسان»، «التحليل النحوي» — dropping the `/qlisan` entry (`Navbar.tsx:21`, the only `ScanText` consumer) and the now-unused icon import
- [x] 3.6c Replace the "Quran RAG" brand in both the sidebar header (`:79`) and the mobile top bar (`:52`) with «القرآن بالقرآن»
- [x] 3.6d Flip the active nav item's gradient at `Navbar.tsx:102`: `bg-gradient-to-r` is a physical `linear-gradient(to right, …)` and never follows `dir`, so after the side flip the pill's dark end sits under the icon and label. Use `bg-gradient-to-l` so `from-brand` stays at the row's leading (right) edge
- [x] 3.7 Flip the mobile drawer's hidden transform from `-translate-x-full` to `translate-x-full` (transforms are physical — no logical utility does this; design D3)
- [x] 3.8 Verified 2026-09-08 — `baseline/after-step3-nav-right.jpg`. Sidebar pinned right on md+, five Arabic entries reading right-to-left within each row, no `/qlisan` link, brand «القرآن بالقرآن», the active pill's gradient now starting at the row's leading (right) edge, and the content offset clear of the sidebar.
      **The drawer transform is verified by measurement rather than by eye**, which matters because a transform is the one directional property no logical utility governs (design D3). With the transition disabled to remove the timing confound, on a 1470 px viewport: no transform → the aside spans 1214-1470 (visible at the right edge); `translateX(+100%)` → 1470-1726, i.e. **fully off-screen past the right edge**, the correct hidden state and what the source now uses; `translateX(-100%)` → **958-1214, sitting directly over the content area** — the D3 trap, quantified at 256 px of overlap.
      *Gap, recorded rather than glossed:* the actual `md:` breakpoint behaviour below 768 px was **not** observed. The browser-resize tool reported success without changing the viewport (1470 px throughout), so the drawer was never rendered at a width where its base transform applies. The transform's direction is measured; the breakpoint interaction, the backdrop click and the close button at mobile width are deferred to task 9.2
- [x] 3.8b Nine-route walk done 2026-09-08 at desktop width against the worktree dev server; evidence in `baseline/after-step2-*.jpg`, comparable against the `before-*` captures. The intermediate state is broken as predicted, and the walk turned four predictions into observations — two confirmed, one corrected, one refuted:
      · **Already correct by document direction alone:** the chat send button and the Verse Study search button both moved to the **left** of their input with no DOM change — the placement requirement is met by the root flip, as designed. The Verse Study tab strip now reads right-to-left with the `word` tab rightmost. `/fassila` is unchanged, having been RTL already.
      · **Finding A6 confirmed by direct comparison:** the Verse Study root bar inverted — «الجذر رحم» moved from the right (`before-verse-study-word.jpg`) to the **left** (`after-step2-verse-study-word.jpg`), contradicting the code's own comment. Task 5.3b stands as written.
      · **Finding A13 confirmed in mechanism, overstated in severity:** both user and assistant bubbles now stack on the right, so the left/right speaker convention is gone — but the green/grey colour coding still carries the distinction. Degraded, not "unreadable as a dialogue". Fix in 5.2; not a blocker.
      · **Design D19 confirmed decisively, and it is the worst of the four:** a French answer renders with the previous sentence's period migrated to the head of the next line (`.patients. C'est une phrase…`), and a French question renders as `? Et en français, que dit le verset 2:153`. Latin content in an RTL block with no isolation. Tasks 5.2b and 6.2c are the fix.
      · **Finding B10 confirmed:** `HealthBanner` shows `.Backend unreachable — start it with ./local-dev/start.sh` — period rejected to the left, and a script path a cloner does not have.
      · **D22's disproof confirmed after the fact:** with `lang="ar"` now on `<html>`, every digit on `/verse-study` is still Western (`336`, `134`, `﴿1﴾`, `﴿37﴾`). The substitution did not arm.
      *Not covered by this walk:* mobile width, and the `/qlisan` and `/tahlil` verse pickers — deferred to task 9.2, since both are edited in group 5 before any final walk anyway.
## 4. The Arabic dictionary

- [x] 4.1 Create `src/lib/strings.ts` as one typed `as const` object grouped by page/component
- [ ] 4.1b Every label map SHALL be keyed by a **closed union**, never `Record<string, string>`, and SHALL NOT fall back to a machine identifier. Close the three open maps: `tahlil/page.tsx:18-24` `BLOCK_EN` (accessed at `:384` as `BLOCK_EN[id] ?? id`, so a missing key renders the raw Latin key `tarkib`), `MadarAslCard.tsx:25-27` `SOURCE_DISPLAY` (renders `maqayis_openiti`) and `:28-30` `EDITION_DISPLAY` (renders `Harun_DarAlFikr`). `TahlilClaim.tsx:7` and `StatusBadge.tsx:10` already do this correctly — follow them
- [x] 4.2 Write the navigation and page-name entries, using the names settled in group 1
- [x] 4.3 Write the per-page entries: headings, captions, tab labels, buttons, placeholders, ARIA labels, empty and loading states
- [x] 4.4 Write the `errors` group. Expose `errors.forStatus(status, kind)` returning a **distinct** Arabic sentence per failure kind rather than one generic sentence per call site, so the actionable information the backend already sends is not demoted along with the English (design D17)
- [x] 4.5 Express every interpolated string as a function so no Arabic sentence is concatenated in JSX (design D5)
- [x] 4.5b Add `iso()` — an FSI…PDI wrapper (`⁨`…`⁩`) — to `strings.ts`, and apply it to every interpolated Latin or numeric token. A dictionary function returns a `string` and therefore *cannot* emit the `dir="ltr"` span the risk register promised; FSI/PDI is the only isolation available to a plain string, and it also works inside `title` and `aria-label`, where an element cannot go. Grep the finished file for any `${` not passed through `iso()` and justify each (design D15)
- [x] 4.5c Add `count(n, forms)` implementing العدد والمعدود for the UI subset: `n === 1` singular; `n === 2` dual (آيتان/آيتين، فاصلتان/فاصلتين، سورتان/سورتين، أصلان/أصلين); `3 ≤ n ≤ 10` plural (آيات، فواصل، سور، أصول); `n ≥ 11` singular accusative (آية، فاصلة، سورة، أصلًا). Route every count through it. Call sites: `verse-study/page.tsx:716`; `FassilaBars.tsx:33,50`; `FassilaDiversityLine.tsx:251`; `FassilaDistributionPie.tsx:187`; `FassilaAnalysisTab.tsx:109-111`; `MadarAslCard.tsx:17-22` — whose `n >= 4` branch is already wrong for `n ≥ 11` («١١ أصول» must be «١١ أصلًا») and is the repo's own precedent for the rule (design D18)
- [x] 4.5d `relativeTime()` now calls the dictionary, so the Arabic noun form follows the count through `count()` — «قبل دقيقة» / «قبل دقيقتين» / «قبل ٥ دقائق» / «قبل ١١ دقيقة», and the same for the ساعة and يوم series. Four English strings that no task covered before the review

- [ ] 4.5e Mechanise the three decidable classes the review gate should not be carrying, so 4.6 is left with register and idiom only: (a) a Vitest case asserting `count()` at n = 1, 2, 5, 11 for each noun series; (b) an assertion that no `${` in an Arabic template escapes `iso()`; (c) the Latin-residue grep over `strings.ts` (the (d) pass of 7.10c), run here rather than at the end. A human cannot check these more reliably than an assertion can, and they drown the one thing only a human can judge

> **Drafted 2026-09-08, not wired.** `frontend/src/lib/strings.ts` exists, `npx tsc --noEmit` is clean, and no component imports it — grep confirms zero call sites, so a correction here costs one edit rather than a re-wiring. The 4.5e checks were run by hand on the draft and all pass: no Latin residue in any value; every interpolation of unknown script goes through `iso()` (this caught two real defects — `questionWord` and `questionPhrase` echoed the user's query into mirrored guillemets without isolation, now fixed); and `count()` yields آية / آيتين / 5 آيات / 11 آية across n = 1, 2, 5, 10, 11, 286. The review table is `strings-review.md` in this directory.

- [x] 4.6 **Arabic review gate — blocking core.** Reviewer: **the owner** (single signatory). Signed **2026-09-08**, whole file accepted as drafted, no corrections. Group 5 is unblocked.
      Scope: the ~25 strings whose reach is cross-page, where a late correction is most expensive — the application name, the five navigation entries, the three Verse Study tab labels, the landing copy, and the shared vocabulary of the `errors` group. Read from `strings-review.md` in this change directory (key · Arabic · original English), not from the TypeScript.
      Record the outcome as a checklist appended to that file — one line per key group, accepted / corrected — with every correction applied to `strings.ts` before sign-off. **Group 5 does not start until this line is `[x]`.**
- [x] 4.6b **Rolling review — per page, non-blocking.** Pre-signed 2026-09-08 with Part A, so no group-5 commit waits on a reading of strings already drafted. Task 4.6c remains the channel for anything the wiring adds or changes. The strings local to one page are reviewed when that page is wired, at its own commit in group 5. Same three-column format, appended to the same file. This covers the page headings and captions, buttons, placeholders, empty and loading states, plus the eight Arabic templates that stay in components — three of them `title`/`aria-label` text no visual pass can see: `FassilaDiversityLine.tsx:251`, `FassilaLine.tsx:178`, `FassilaDistributionPie.tsx:187`, `FassilaBars.tsx:33`, `SarfiRows.tsx:63`, `qlisan/page.tsx:293-294`, `TahlilClaim.tsx:89`, `MadarAslCard.tsx:17-22`
      *Why splitting is safe:* the vocabulary that could diverge between two pages — page names, navigation, tab labels — is precisely what the blocking core in 4.6 holds. What is left is local to one screen
- [ ] 4.6c Re-review gate: any Arabic string added or changed during groups 5-7 goes back through the rolling review before 9.1. Record the delta, not the whole file.
      **Queue as of the end of group 6 — 15 strings.** (a) the three card CTAs «ابدأ محاورة» / «ادرس كلمة» / «حلِّل كلمة»; (b) `chat.exampleHint`, `chat.retry`; (c) `verse.loadingSurah`, `verse.loadingVerse`; (d) three accessible names, which no visual pass can review: `chat.inputLabel` «سؤالك», `verseStudy.wordLabel` «الكلمة», `verseStudy.phraseLabel` «الآية أو العبارة»; (e) `lexical.noOccurrences`, a sentence rather than a label — «لم يرد للجذر … موضعٌ في القرآن»; (f) four `NOUNS` entries whose *dual and plural* are the part to check, since they are what the interface will print: `mawdi`, and the three phrase-forms `fasilaMumayyaza` / `ayaMuhallala` / `ayaMuqattaa` (design D23)

## 5. Pages: strings and direction

- [x] 5.1 `app/page.tsx` rewritten 2026-09-08 — `baseline/after-5.1-landing.jpg`. Hero heading, lede, three feature cards and the closing note all read from the dictionary; zero Latin text remains (verified by the JSX-text and visible-attribute passes of 7.10c on this file). The "Arabic, French, or English" claim is dropped from the copy while the capability remains in the backend — a change of emphasis, which is exactly why task 5.2b still has to isolate the answer bubble.
      The hero's primary button is «ابدأ المحاورة» rather than a repeat of the page name, and being first in source order it lands at the row's leading (right) edge. The three cards read right-to-left in the navigation's own order.
      **The CTA arrow's glyph is flipped here, not deferred to 7.7:** `ArrowRight` → `ArrowLeft` with `group-hover:-translate-x-0.5`, because leaving a knowingly-backwards arrow in place across several commits is less attributable, not more. The `page.tsx:83` entry of task 7.7 is therefore already discharged.
      *Three new strings* — the per-card action labels «ابدأ محاورة» / «ادرس كلمة» / «حلِّل كلمة» — were not in the reviewed dictionary and are a **task 4.6c delta** awaiting review.

- [x] 5.2 `components/ChatInterface.tsx` wired 2026-09-08 — `baseline/after-5.2-chat.jpg`. Eighteen replacements, each asserted to match exactly once so a silent no-op was impossible: the switcher, the empty state, the sources label, the per-answer affordances, the retry button, the composer placeholder, and the directional utilities (`text-right`→`text-end` on the bubble, `text-left`→`text-start` in the switcher, `ml-auto`→`ms-auto` on the feedback row, `ml-1`→`ms-1` on the inline spinner).
      **The `text-end` fix does more than mirror.** Under RTL it puts the user's own bubbles on the **left** and the assistant's on the right — the inverse of the Western habit and the correct convention for an Arabic reader, whose own turns belong at the reading direction's arrival edge. The point was never "put the user back on the right"; it was to let the document direction decide the side, which `text-right` prevented.
      *Deferred by design:* the `e?.message` at `:231` stays English until task 5.9, which applies one shape across all twelve sites in its own commit.
      *Two new strings* — `chat.exampleHint` and `chat.retry` — are a **task 4.6c delta** awaiting review.

- [x] 5.2b `dir="auto"` on the message content and on the composer textarea — **and it is the most visible fix of the change so far.** Before: the seeded French answer rendered `.patients. C'est une phrase…`, the previous sentence's period migrated to the head of the next line, and the French question rendered `? Et en français, que dit le verset 2:153`. After: the answer ends `…document RTL.` with its punctuation in place and the paragraph laid out left-to-right, and the question recovers its trailing `?`. Compare `baseline/after-step2-chat-transcript.jpg` with `baseline/after-5.2-chat.jpg` (design D19)

- [x] 5.2c The icon-only send button now has an accessible name («إرسال»). It had **none at all** before, so the locale requirement could not have been met by translating one — it had to be added

- [x] 5.3 `app/verse-study/page.tsx` wired 2026-09-08 — `baseline/after-5.3-verse-study.jpg`. Heading, caption, the three tab labels, both placeholders, both `title` tooltips, the similar-search note, the context caption, the load button, the empty state and the root/proper-noun labels all read from the dictionary. No Latin text remains on the page. The `similar` label «الآيات القريبة في المعنى» fits the three-tab strip at desktop width, so the pre-approved shorter form is not needed.
      Also fixed here: the sūra select had **no accessible name at all** while its twin on `/tahlil` has one, and the counted noun at `:716` rendered «أقرب 5 آية» — now routed through `count()` (design D18).
      *Deliberately not done here:* the five `text-right` sites in this file. All are RTL-context or cross-boundary cases, and design D12 requires them classified once, per site, in group 7 — not improvised page by page.

- [x] 5.3b The third hand-reversed row is restored to logical order — root span first, totals second — so the document direction is what puts «الجذر» on the right. Verified against `baseline/after-step2-verse-study-word.jpg`, where it had inverted to the left.
      **A coupling the task did not anticipate, and nothing mechanical would have caught:** `flex-col-reverse` on the stacked variant existed *because* the DOM was reversed. With the children swapped it would have put the totals on top and the root at the bottom — the block axis being direction-independent, it does not flip with RTL but it does depend on child order. It is now `flex-col`, and the comment records the coupling so the next reader does not undo it. `items-end` → `items-start` as planned, that one being a cross-axis alignment which does flip.

- [x] 5.3c Isolation added: `dir="auto"` on both echoed-query spans, `dir="ltr"` on the parenthesised sūra number in the group header (`(` and `)` are bidi-mirrored, so their ordering depended on neighbours), and the `/ {maxAyah}` hint wrapped and given `.western-digits` — discharging that site of task 5.4c

- [x] 5.4 `app/qlisan/page.tsx` wired 2026-09-08, and its verse picker restored to logical source order — sūra select, āya box, load button — with `justify-end` removed, since under RTL it would now mean the *left* edge. Done in full even though the page has left the navigation, so a direct URL does not reach a half-migrated page (design D11). The sūra select also gained the accessible name it lacked

- [x] 5.4b The fourth faked-RTL row — the loaded-verse card header — is un-reversed: source order is now sūra name then reference, and `justify-end` is gone. Finding A7, which the original D4 had missed while fixing its two siblings

- [x] 5.4c All three `/ {maxAyah}` hints wrapped in `dir="ltr"` and given `.western-digits` (`qlisan`, `tahlil` here; `verse-study` in the 5.3 commit). Verified on screen: the hint reads `/ 118` with the slash before the number, rather than reordering to the far side of the digits

- [x] 5.5 `app/tahlil/page.tsx` wired and its picker restored identically — `baseline/after-5.5-tahlil-picker.jpg`. **The requirement is now met visibly and for the right reason:** the row reads sūra «23. المؤمنون», then āya `61`, then `/ 118`, then the load button at the left end — produced by the document direction, with source order select → input → button. The rendered result matches what LTR-plus-the-trick produced before; only the source became honest

- [x] 5.6 `app/lexical/page.tsx` wired 2026-09-08 — `baseline/after-7-lexical-sweep.jpg`, which doubles as the sweep's visual proof: «حلِّل» sits at the **left** of its input, the root card shows «الجذر رحم» on the right with the searched word pushed left by `ms-auto`, each letter card's confidence pill is at its left, and the «القراءة التتابعية» chain reads ر → ح → م right-to-left with the new SVG arrows pointing left between the cards. The word field also gained the accessible name it lacked — its only name was the sample placeholder «رحمة»

- [x] 5.7 Closed by inspection: `fassila/page.tsx` carries no `dir` attribute at all — the container `dir="rtl"` went with the 45 deleted in task 7.9, after the sweep had read it as annotation. Its Arabic tab labels are untouched
- [x] 5.8 Both deep-link pages done, in the same commit as their icons — separating the two concerns across a handful of lines in two 110-line files would have bought no attribution. Strings wired (loading states, pagination labels, the back link); the sūra subtitle's English and French names are **dropped rather than translated**, since the Arabic name is already the `h1` and the locale spec resolves a two-script field in favour of Arabic; prev/next references wrapped in `dir="ltr"`
- [x] 3.4c The duplicate `toArabicDigits` in `surah/[number]/page.tsx` is deleted and the helper imported from `@/lib/arabicDigits`, so the numeral policy is expressed once in code. This was the substantive half of what D22 uncovered

- [x] 5.9 Done 2026-09-08 — one shape at all twelve sites, plus the infrastructure it needed.
      · **`ApiError` in `lib/api.ts`.** `forStatus(status, kind)` needs the HTTP status, and it was buried inside the message text. All 23 throws now carry it; the English messages are unchanged, because under D10 they become the subordinate detail and never the sentence.
      · **`components/FailureNote.tsx`** is the one way this app reports a failure: the Arabic sentence, and below it the exception text inside `dir="ltr" lang="en"` — the `dir` so it lays out left-to-right, the `lang` so a screen reader does not spell English out with an Arabic voice. The chat's retry button rides along as a child.
      · Verified on a real 404/422: the reader sees «تعذَّر إتمام الطلب.» and the developer keeps `Verse lookup failed: 422` on a muted monospace line beneath it.
      **The finding that matters most here: `catch (e: any)` had disabled the typechecker.** One of the twelve sites — the per-verse context map in `verse-study` — still assigned a bare string into a `Record<string, Failure>`, and `tsc` passed, because `e?.message` is `any` in an `any`-typed catch and `any` is assignable to anything. So the conversion of these sites was **not** compiler-verifiable, contrary to what the typed-dictionary requirement promises. Two consequences, both acted on: the missed site is fixed, and all 7 `catch (e: any)` clauses are narrowed to `unknown` so the compiler now guards them. Proven rather than assumed — reinjecting the old shape now yields `TS2345`, where before it was silent.

- [x] 5.9b Discharged by `FailureNote`, which declares both `dir="ltr"` and `lang="en"` on the detail line in one place rather than at twelve call sites

- [ ] 5.9c The **4 remaining** `message ||` sites are a different class and 5.9 does not fix them: `qlisan:367`, `qlisan:407`, `LisanResult:47`, `LisanResult:252` render a **backend** `message` field as the primary sentence. Three already fall back to Arabic; `LisanResult:47` does not, and its backend value is English (design D16, task 6.3c)
- [x] 5.10 / 6.1b The default conversation title is «محادثة جديدة» — **and the persisted ones are handled, which is the part that mattered.** Translating the constant leaves English in every store already written to a reader's browser, so `loadStore()` now maps the exact legacy value on read. Deliberately *not* a version bump: `loadStore()` returns an empty store on a version mismatch, so bumping it would silently delete every saved conversation. `STORAGE_KEY` is untouched for the same reason. Two new tests lock both halves — the legacy default is mapped, and a title the reader's own message produced is left alone
- [x] 6.1 `HealthBanner.tsx` — the three sentences, with all three corrections beyond translation. The unreachable message **names no script path at all**: the English original pointed at `./local-dev/start.sh`, which is git-excluded, so a cloner does not have it — and naming nothing is more useful than naming a file that may not exist. The degraded list joins with `" و"`, a space *before* the waw and none after, because it is a proclitic and `join(" و ")` is wrong Arabic. Each Latin brand name carries its own FSI/PDI isolate from the dictionary, without which «(Qdrant)» renders «)Qdrant(»

- [x] 6.2 Both cards: the reference islands the spec required and neither had. `VerseCard`'s `({surah}):{ayah}` and `[{id}]` are wrapped, and the brackets go **inside** the island — `(` `)` `[` `]` are bidi-mirrored, so their orientation depended on whichever script preceded them. `VerseContextCard`'s bare `N:N` is wrapped too: on its own it is bidi-safe, but the spec asks for a declaration rather than an accident a neighbouring edit could break. Its āya badge now goes through `toArabicDigits` — it is a reading number
- [x] 6.2b `VerseCard` no longer leads with Latin. It preferred `surah_name_en` — the *optional* field — over `surah_name_ar`, the required one, on the most-rendered component in the product. «الجزء» and «درجة المطابقة» are wired with `.western-digits`, and `period` is mapped through the dictionary: an unmapped value now renders **nothing** rather than leaking the corpus id, per the closed-map principle of 4.1b
- [x] 6.2c Both translation paragraphs carry `dir="auto"` and their `lang` — the `dir` so a French or English paragraph is not laid out RTL, the `lang` so a screen reader does not read it with an Arabic voice (design D19)
- [x] 6.3 `SarfiRows` and `FicheRow` needed no directional edit, as predicted: both are RTL-native, and `SarfiRows.tsx:134` («`items-start` = right edge in RTL») is the comment the corrected D12 was measured against. `LisanResult`, `MadarAslCard` and `LevelCard` were done in the previous slices
- [x] 6.3b `titleEn` removed — the prop, its six call sites, the pass-through wrapper, the now-dead `BLOCK_EN` map (which also settles one of the three open `Record<string, string>` maps of task 4.1b), and the hand-rolled twin «Grammatical» in `LisanResult`. An uppercase Latin gloss beside every Arabic level heading is decoration in the wrong script, not an exempt identifier — and being wrapped in `dir="ltr"` is exactly what let it pass both the residue grep and the island rule. Removed rather than translated: the Arabic label is the label
- [x] 6.3c The one sanctioned backend edit (design D16), prose only. The `message` is now the Arabic sentence its sibling `madar/madar_service.py` already returned verbatim — a copy, not a new translation — so the most common non-happy path on «تحليل اللسان» stops being an English paragraph inside an Arabic screen. `SOURCES` cites two Arabic works and now cites them **in Arabic script**, which is the same value in its own script rather than a translation of an identifier, with their glosses in Arabic. `LisanResult`'s English fallback goes with it.
      *Checked before editing:* no local Python test pins either string. `tests/test_tahlil_huruf.py:495` does assert that an author name carries Latin — but that is `raw["source"]["author"]` in the tahlil/ḥurūf dataset, a different field in a different dataset. Recorded so the two are not conflated later
- [x] 6.4 All three closed. `ScrollToTop.tsx:29` is `end-6` and its two Arabic names are in place, so the button now sits bottom-**left**, clear of the navigation that took the right — the one place where the mirror table would have preserved pixels and lost the intent (design D12). `StatusBadge` has neither a string nor a directional utility (its `title` comes from the caller), and `TahlilClaim` was already Arabic throughout, `KIND_LABEL` included; `flex items-start justify-between` is direction-neutral
- [x] 6.4b Done — and done earlier than planned: all six moved with their pages in group 5, so the audit found **zero** English `aria-label`, `placeholder`, `title` or `alt` left in `frontend/src` outside the tests. Recorded rather than repeated
- [x] 6.4c The scan had to be rewritten before it could be trusted: matching an element as `<input((?:[^<>]|\{[^{}]*\})*?)>` truncates at the first `>` of an arrow function, so the first pass reported eleven unnamed controls out of eleven. Reading each element up to the line that closes it gives the real figure: **11 controls, 7 already named**. The two sūra selects this task named had been given `aria-label={S.verse.surah}` in group 5, and `FassilaAnalysisTab.tsx:65` is named by a real `<label htmlFor>` — recorded, since an audit that only greps for `aria-label` would call it a violation.
      The four genuinely unnamed: `FassilaAnalysisTab`'s select (named by its label, above) and **three text controls whose only name was a placeholder** — `verse-study` word and phrase, and the chat composer. A placeholder is the *last* fallback in the accname spec and it disappears on the first keystroke, so the field is unnamed exactly while it is in use. Each now carries a short `aria-label` that names the field rather than repeating the hint, since the label would otherwise cost the user the example
- [x] 6.5b Fixed, and **verified in the running page**: the four rows of sūra 1 now read «93 آية · 83.8%», where before the fix `/fassila` showed «%83.8». Read back by sorting the row's characters on their rendered x — the `%` is to the right of the digits. One `dir="ltr"` span was enough, and the `title` at `:33`, where no element can go, takes the same token through `iso()`. Recorded as **pre-existing**, so task 9.3 does not attribute it to this change
- [x] 6.5 Nothing to translate: **the four Fāṣila components carry no Latin user-visible string at all** — the JSX-text and attribute passes both return empty over `components/Fassila*.tsx`. Measured rather than assumed; the feature was born Arabic. No physical utility survives there either, the sweep having taken all of them, so the task reduces to the two defects the reviews surfaced (6.5b, 6.10)
- [x] 6.6 Strings only, and there were none — see 6.5. No axis code and no utility inside an existing `dir="ltr"` wrapper was touched (design D7)
- [x] 6.6b `dir="ltr"` added to `FassilaLine.tsx:89`, satisfying the already-in-force requirement `openspec/specs/fassila-surah-comparison/spec.md:589` that `fassila/page.tsx` violated. Verified in the page: the scroll wrapper reports `dir="ltr"` and `scrollLeft = 0`, so the sequence opens at āya 1. `FassilaDistributionPie` needs none — no axis and no horizontal scroll — recorded as a decision, not an omission
- [x] 6.6c Both tooltips stay right-to-left (their text is Arabic; the spec's "tooltips" is amended so it cannot sweep them in), `FassilaLine.tsx:189` gains the `font-arabic` its sibling already had, and the hardcoded `Math.min(x + 14, 1200)` clamp is replaced in both by `lib/chartTooltip.ts`. `1200` was never the viewport nor the content area; after the navigation moved it is wrong in a new way, the content area now ending 16rem early while the tooltip's `z-20` loses to the navigation's `z-50`. Past the middle of the content area the tooltip is anchored by its own far edge (`translateX(-100%)`), which is exact without measuring its width — the reason for anchoring rather than subtracting a guessed width
- [x] 6.6d Decided, and the decision changed shape once measured: that `dir="ltr"` was doing **two jobs**. Removing it moves the controls, and it also breaks the «×2» label — measured by sorting the rendered characters on their x, «×2» becomes «2×» in an RTL run, the multiplication sign being a neutral that takes the paragraph direction. So the island moved **down to the label that needs it** and the row is placed logically instead: `justify-start`, which in RTL is the physical right — today's pixels, now stated as intent (the controls belong to the card, so they sit at its start edge). Verified in the page: both zoom labels render «×1»
- [x] 6.6e Kept right-to-left, no island added. Verified in the page: the comparison table's scroll container reports no `dir` and `scrollLeft = 0`, so it opens on «السورة», its first column. The archived requirement is scoped to "a chart whose X axis reads left-to-right", which a table is not — recorded as a decision so it is not re-read later as an oversight
- [x] 6.7 Translated — **five** strings, not the three the task counted: the not-found sentence, «الجذر», the occurrence count, «التحليل» and «آياتٌ شاهدة». The not-found case needed a new string rather than `noRoot`: there a root *was* resolved and simply has no attestation, so «لم يُعرف جذر» would state something false. Its `ml-auto` is `ms-auto`, not the `ms-auto` this task predicted from the superseded two-table wording nor the `me-auto` the sweep applied — see 7.12, where the same measurement settles both
- [x] 6.8 Nothing to change: task 1.5 settled that the canonical Arabic renderer keeps **both** `dir="rtl"` and `lang="ar"`, its direction being part of its contract rather than a restatement of the document's (design D9), and task 7.9 already exempted it when deleting the other 45. Closed as a decision so the file does not read as an oversight in the final audit
- [x] 6.9 Left intact, as designed: all 23 English `new Error(…)` messages stay, becoming the subordinate `dir="ltr"` detail line under D10 — the fourth exempt class the locale spec names. What did change here is structural, not textual: `ApiError` now carries the HTTP status (previous slice), which is what lets a caller pick an Arabic sentence without parsing English
- [x] 7.1 Annotation produced 2026-09-08 — and **measured in the browser rather than reasoned from the JSX.** A site's pre-flip direction is exactly "does it have an explicit `dir="rtl"` ancestor", since the root was LTR before; `closest('[dir="rtl"]')` answers that per element, far more reliably than inferring nesting from indentation. Results: every Fassila site is RTL-context; on `/verse-study` the surah- and lemma-card headers carry `dir="rtl"` themselves (142 elements) while the verse buttons do not (336), and both classes convert identically. Inventory at the time of the sweep: **28 occurrences over 14 files** — down from 36/16, the page work of group 5 having already converted the rest

- [x] 7.2 / 7.3 **Converted with ONE table, the mirror one — design D12 is corrected.** Measured in the running page: in an RTL container `text-align: right` renders RIGHT, `start` renders RIGHT, `end` renders LEFT. So `text-right` *is* `text-start`, uniformly, and there is no second table. The correction makes the original defect worse rather than milder: task 7.2's first mapping (`mr-`→`me-`, `text-right`→`text-end`) would have inverted **all 24** convertible sites, not two thirds of them, because nothing outside an LTR island still resolves LTR after the flip.
      24 occurrences converted across 12 files, each replacement asserted for an exact count. One deliberate exception, recorded as an intent decision rather than a conversion: `ScrollToTop`'s `right-6` becomes `end-6`, not `start-6`, so the control lands bottom-**left** and clear of the navigation that now occupies the right


- [x] 7.4 Cross-boundary alignment: **one reviewer claim did not survive contact with the code.** `VerseCard.tsx:63` and `VerseContextCard.tsx:59` were reported as cross-boundary — alignment on an LTR ancestor inherited by an RTL descendant — but `ArabicText` merges the incoming `className` onto its *own* `dir="rtl"` span, so the alignment lands on the RTL element itself. The conclusion (never write `text-end` there) was right; the mechanism was not. `verse-study/page.tsx:219` is genuinely a button with an RTL child, and under the corrected single table it converts the same way as everything else, `text-start` inheriting consistently

- [x] 7.5 Direction-aware audit done. Remaining after the sweep: 10 `items-start` (RTL-native, documented at `SarfiRows.tsx:134`), 4 `ms-auto` and 2 `me-auto` (the converted ones), 1 `justify-end` — inside `FassilaDiversityLine`'s LTR island, which is task 6.6d's open decision — and 1 `items-end`.
      **That last one is the finest case in design D13 and no grep can classify it:** `ChatInterface.tsx:445`'s `items-end` sits on a *row*, where `align-items` works on the block axis and is therefore vertical and direction-neutral. The one at `verse-study:403` sat on a *column*, where the same class works on the inline axis and does flip. Same utility, opposite sensitivity, decided only by the parent's `flex-direction` — which is why this audit cannot be mechanised the way the mirror table can

- [x] 7.6 No-logical-form properties enumerated, empty results included so the check is repeatable. **Verified zero** for `bg-gradient-to-r` (converted at step 3), `origin-left/right`, `float-*`, `inset-x-`, `space-x-`, `divide-x`, `scroll-m/p-l/r`, `text-indent`, `clip-path` and `background-position`. Present and decided: 4 transforms (`Navbar` ×3, `page.tsx` ×1), 1 gradient (`bg-gradient-to-l`, flipped at step 3), and 2 inline `style={{ left }}` on the chart tooltips (clamp deferred to task 6.6c)

- [x] 7.7 Seven glyphs mirrored — icons are SVG paths and do not mirror with `dir` (design D14): `surah/[number]` prev/next swapped, `verse/[surah]/[ayah]` prev/next swapped, `VerseContextCard`, `qlisan`, and `ChatInterface`'s `Send` flipped with `-scale-x-100` because the icon set offers no mirrored paper plane. `page.tsx` was already done in the 5.1 commit

- [x] 7.8 The four RTL-correct glyphs are confirmed untouched: `FassilaAnalysisTab.tsx:62` (`ChevronRight`, «السورة السابقة») and `:83` (`ChevronLeft`, «التالية»), `verse-study/page.tsx:220` and `:553` (collapsed disclosures). Their Arabic labels are the proof they were authored for RTL — and they are why a blanket glyph swap would have fixed one half of the tree and broken the other

- [x] 7.8b Both navigational `←` literals replaced by SVG icons, so their direction is a decision rather than a renderer behaviour: `LisanResult.tsx:150` (the «القراءة التتابعية» chain separator, pointing left along the row) and `verse/[surah]/[ayah]/page.tsx:59` (a "back" affordance, pointing right). `FassilaAnalysisTab.tsx:152-153` is left alone as planned — those are "becomes" arrows inside Arabic methodology prose, not navigation — and this records that they were checked rather than missed

- [x] 7.9 **45** redundant `dir="rtl"` attributes deleted, last as the corrected D12 requires, and committed with the sweep. Two kept by decision: the root declaration in `layout.tsx`, and `ArabicText.tsx` — the canonical Arabic renderer, whose direction is part of its contract rather than a restatement (design D9). Verified before deleting that this was safe: every `dir="ltr"` island in the tree is a leaf span (a reference, a QAC tag, a digit hint, a technical detail line) or one of `FassilaDiversityLine`'s chart wrappers, so no `dir="rtl"` was re-establishing direction inside an island

- [x] 7.10 Residue check passed, with a **better exit criterion than the original**. Re-running the 2.1 command verbatim leaves exactly **4 occurrences, all transforms** — the one class of directional property with no logical form at all. Zero physical *layout* utilities remain. The task originally asked for "every remaining physical utility inside a declared LTR island", which was unachievable: the transforms are not in islands and never can be, because they are not a layout side but a painting operation

- [x] 7.10b Discharged by the sweep and by measurement: `FassilaDistributionPie.tsx:188` (legend button) and `FassilaBars.tsx:49` (stats column) both measured **RTL-context**, so they converted with everything else. They are flow-layout chrome, not axis geometry, and sit outside the chart's LTR island — recorded as a decision rather than left as an exception

- [x] 6.10 **The counted nouns that were already Arabic** (design D23). `/fassila` displayed «2 آية» and «1 آية»: ten sites across five Fāṣila components interpolate a count in front of a fixed noun, which the locale spec's own requirement forbids — and which every language sweep walked past, the string being Arabic. Two carry an agreeing adjective and are stored as whole phrases in `NOUNS`; three style the numeral apart from the noun and go through a new `Counted` component over a new `countParts()`, so the rule is never re-decided in JSX. Verified in the running page across three sūras: «93 آية», «9 آيات», «آيتين», «آية», «3 فواصل», «10 سور», and «استُبعدت آية مقطّعة من التحليل · 285 آية محلَّلة». One site is an `aria-label`, which the spec had already anticipated as a scenario before the implementation found one
- [x] 7.12 **The mirror table's one wrong case, found in group 6 and fixed across the tree.** An auto margin on a flex item is not a position but the side of the main axis that absorbs the free space, so mirroring it produces no push at all — measured on `VerseCard`'s header, where `me-auto` renders pixel-identical to no class (527 px) against `ms-auto`'s 0. Six sites; the four the sweep left as `ms-auto` were right and the two it turned into `me-auto` were inert. D12 amended: for auto margins the ORIGINAL two-table rule holds, and `text-align` — the utility the single-table correction was measured on — cannot exhibit this
- [x] 7.10c Four passes, all clean, run over `frontend/src` excluding `*.test.*`:
      (a) JSX text nodes — `grep -rnoE '>[A-Za-z][A-Za-z ,.?!-]{2,}<'` → **0 hits** (10 before this change).
      (b) user-visible attributes — `grep -rnoE '(placeholder|aria-label|title|alt)="[^"]*[A-Za-z]{2,}'` → **1 hit**, «QAC» inside an Arabic `title` at `LisanResult.tsx:67`: an exempt identifier, the corpus's own name.
      (c) Latin sentences reachable only through a variable — every `setError(` / `setMessage(` / `let …: string` initialiser. `HealthBanner.tsx:38`, the site this pass exists for, now assigns from `S.health.*` in all three branches. One deliberate survivor: `conversations.ts:17`'s `LEGACY_DEFAULT_TITLE = "New conversation"`, which is never rendered — it is the value **compared against** what a previous version wrote to a reader's disk.
      (d) `strings.ts` itself → the only Latin in its string *values* is inside `iso()` isolates by design (`(Qdrant)`); everything else the grep finds is comment prose or a TypeScript key.
      Confirms the two violations this task predicted are gone: `LisanResult`'s «Grammatical» and `LevelCard`'s `titleEn`, both islanded and both violations — a `dir="ltr"` wrapper never made a string exempt
- [x] 7.11 The D11 collision is resolved, both sides moving as decided: the page is «التحليل النحوي» and the section at `LisanResult` is now **«الصرف والإعراب»** — which is what it actually renders, `SarfiRows` being morphology plus a case-marker hint from the نحوي level, so its old label was loose independently of the collision. `LisanResult.test.tsx`'s hard-coded `TITLE` constant went with it, and now reads the dictionary per design D8
- [x] 8.1 All three done. The tab queries read `S.verseStudy.tabs.*`, the link query reads the key `VerseContextCard` renders, and the error test is **rewritten** rather than adjusted: it now rejects `new ApiError("Verse 2:255 not found", 404)` and asserts both that `S.errors.verseNotFound` is on screen and that the element carrying the English message declares `dir="ltr" lang="en"`. The old version asserted only the English text, which under D10 stays on screen as the detail line — so it would have gone green whether or not the Arabic sentence was ever wired
- [x] 8.2 Done where it applies. The scope note held: these suites assert mostly on Arabic domain vocabulary, already exempt under D5(b), so the only chrome assertion that needed the dictionary was `LisanResult.test.tsx`'s `TITLE` — which had to move anyway, being one side of the D11 collision (task 7.11)
- [x] 8.2b All six removed. Worth recording *how* they were found: not by a failing test — React ignores an unknown extra prop, so the suite stayed green while passing a prop the component no longer accepts — but by `tsconfig.test.json` (task 8.6), which is the first thing in this project to typecheck a test file
- [x] 8.2c Rewritten rather than retargeted: the two cases now assert the **legacy-title mapping** — a store seeded with `"New conversation"` reads back as `S.chat.newConversation` and keeps its conversation, while `"New conversation era"` (a title a reader's own message produced) is left alone
- [x] 8.3 `src/app/layout.test.tsx`, 4 assertions, rendered with `renderToStaticMarkup` and parsed with `DOMParser` — never into the ambient document, for the reason the task gives. Only one of the two planned mocks was needed: `next/navigation`, because `Navbar` dereferences `usePathname()`. The `next/font/local` stub is **moot** — design D6 replaced it with self-hosted `@font-face`, so the 0-byte webpack-loader stub is no longer imported at all.
      Beyond `lang`/`dir` it asserts the D1 contract as something countable — exactly one element in the shell carries `dir` — and that the sidebar offset is `md:ms-64` with no `m[lr]-` anywhere. **Proved able to fail**: injecting `dir="rtl"` plus `md:ml-64` on `<main>` fails two of the four
- [x] 8.4 All five rows, DOM order only, split by where the harness already exists: the three Verse Study rows joined its own suite (all three tabs mount at once, so one render covers them), and the chat composer plus the `/tahlil` and `/qlisan` pickers went into `src/app/inputRowOrder.test.tsx`. No geometry, for the reason the task gives — and the composer assertion doubles as the test that 6.4c's `aria-label` is actually wired, since it is what `getByLabelText` resolves. **Proved able to fail**: reversing the `/qlisan` picker in source — the exact defect this change removed — fails it
- [x] 8.5 **11 files, 94 tests, 0 failures** (1.94 s), and `npx tsc --noEmit -p tsconfig.test.json` clean over the same files. Up from the 8 files / 77 tests of the 2.3 baseline: +3 files and +17 assertions, of which 12 are new contract tests (8.3, 8.4, 2.2b) and the rest are rewrites
- [x] 8.6 `tsconfig.test.json` added (local-only, git-ignored beside the suites it covers) and it **earned its place on the first run**. Two things had been invisible: `LevelCard.test.tsx` kept passing six `titleEn` props to a component that no longer accepts them — React ignores an unknown extra prop, `tsconfig` excludes test files, and Vitest transpiles with esbuild without typechecking, so nothing failed — and `MadarAslCard.test.tsx` carried a **pre-existing** type error, its factory claiming to return a complete `MaqayisCitation` while omitting `asl_preamble`. That field is always sent by the backend, defaulting to `""` (`maqayis_store.py:49`), so the type was right and the fixture was wrong; fixing it also makes the fixture faithful to the API. Both clean now
- [x] 8.7 `evidence.md` written in this directory: the 2.1 inventory with its command and both revisions' output, the 8.5 suite summary, the 9.1 build output with its route list and the emitted font hashes, the 7.10c audit table, and a note on what each screenshot establishes. Every assertion added in 2.2b, 8.3 and 8.4 also exists as a scenario in `specs/` — three were added for this slice: the shell's single `dir` carrier, a chart opening at its axis origin, and a symbol that binds to a number (design D20)
- [x] 9.1 Clean. **10 routes**, matching the 2.3 baseline exactly, and no TypeScript error from the typed dictionary. Three ESLint warnings, all `react-hooks/exhaustive-deps` at `FassilaComparisonTab.tsx:49` — verified **pre-existing** by reading the same line in `main`, not assumed to be. And the build confirms what no unit test or screenshot can: all ten vendored `woff2` are emitted with content hashes to `.next/static/media/`, which is design D6's claim
- [ ] 9.2 Walk all **nine** routes (`/`, `/chat`, `/verse-study`, `/fassila`, `/lexical`, `/tahlil`, `/qlisan`, `/surah/[number]`, `/verse/[surah]/[ayah]`) at desktop and mobile widths: navigation right, no English chrome, no horizontally clipped content
- [ ] 9.2b Confirm the navigation shows exactly five entries with no `/qlisan` link, and that `/qlisan` opened by direct URL is fully Arabic and RTL
- [ ] 9.2c Scoped brand check — three commands, all must return zero, because a bare grep cannot pass without breaking the comments-stay-English policy:

      git grep -n "Quran RAG" -- frontend/src ':!*.test.*' ':!frontend/src/lib/api.ts'
      rm -rf frontend/.next && (cd frontend && npx next build) && grep -rl "Quran RAG" frontend/.next/static frontend/.next/server

  `api.ts:1` is a code comment and stays English. The 11 non-frontend hits (`scripts/`, `api/main.py`, `README.md`, `indexing/`, `ingestion/`) are **out of scope**: this is an interface rename, not a repo rename. Wipe `.next` first — its cache holds pre-change artifacts
- [ ] 9.2d Digit pass, now a **confirmation rather than a hunt** (task 2.4 removed the risk): check that the analytical numbers are still Western and that the āya badges are Arabic-Indic **at both** of the sites reconciled in 3.4c. Still done by eye, because the substitution would act on glyphs and not on characters
- [ ] 9.2e Load a **pre-change** `localStorage` payload (a `version: 1` store whose conversation carries `title: "New conversation"`), open `/chat`, and confirm the switcher shows no English default and that a new message re-derives the title
- [ ] 9.3 Compare the three Fassila charts against the 2.2 captures **and** run the 2.2b assertions — sūra 1 still leftmost, sūra 114 still rightmost, tooltips still readable. At mobile width additionally check the **scroll origin**: the 2.2 captures cannot certify it, because `FassilaLine` opens at the wrong end *before* this change too (task 6.6b). Also eyeball what neither the captures nor any test covers: `FassilaTile` hint spacing, the `FassilaBars` digit column, and the `FassilaAnalysisTab` / `FassilaComparisonTab` table headers — all four are 7.3 mirror-mapping sites
- [ ] 9.4 Check every input row at **desktop** width: chat composer, word search, phrase search, both verse pickers — action button on the left of its input. At mobile width these rows wrap (`flex-wrap`), so the button drops to its own line and the placement rule does not bind; check DOM order there instead
- [ ] 9.4b Walk every prev/next control and confirm "previous" points toward the start (right) and "next" toward the end (left), and that the four RTL-correct glyphs of 7.8 are unchanged
- [ ] 9.4c Check native control chrome, which no logical utility governs: on the three `<input type="number">` āya boxes (`qlisan:161`, `tahlil:179`, `verse-study:926`) and the four `<select>`s (`qlisan:176`, `tahlil:195`, `verse-study:916`, `FassilaAnalysisTab:66`), confirm the UA-drawn spinners and disclosure arrow moved to the left without colliding with the text, and that `verse-study:926` — the only āya box without `text-center` — matches its siblings
- [ ] 9.5 Force a backend-down state and confirm the failure path is Arabic, with any English exception text confined to a subordinate `dir="ltr" lang="en"` line. Then force a 422 by submitting Latin script to «تحليل اللسان» and confirm the Arabic sentence still says what to fix (design D17)
- [ ] 9.6 Keyboard pass: tab order follows the visual right-to-left reading order, drawer focus behaves. Neither tab strip is an ARIA `tablist` — they are plain buttons, so traversal is Tab through DOM order and no arrow-key handler needs mirroring
- [ ] 9.7 Because group 8 cannot cover the mirroring axis at all, make the manual pass explicit and per-site: for every site listed in 7.3, 7.4, 7.5, 7.7 and 5.3b, record the side it rendered on before and after. A screenshot diff is insufficient on its own — it cannot see a defect that pre-exists the change

## 10. Test churn absorbed as it happened

- [x] 10.1 `verse-study/page.test.tsx` updated in the 5.3 commit rather than deferred to group 8. Nine of its fifteen tests broke the moment the tab labels became Arabic, and the Migration Plan's gate is `npx vitest run` green **after every step** — so leaving them red across later commits would have retired the gate exactly when it starts earning its keep. Per design D8 the queries now import `S.verseStudy.tabs.similar` and both placeholders from the dictionary instead of retyping literals: an assertion that retypes the Arabic passes only until someone fixes a hamza, whereas importing the key checks the *wiring*, which is what can regress. 77/77 green again.
      Still owed to task 8.1: the `/Open full Sourate page/` link query, blocked on task 6.2.
- [x] 10.2 The deceptive error test is rewritten (task 8.1c), and its failure earned its keep. It used to mock `new Error("Verse not found")` and assert that same string — echoing its own mock. It now throws an `ApiError(…, 404)` and asserts two things the old one could not: that the sentence on screen is `S.errors.verseNotFound`, i.e. chosen by failure **kind**; and that the English text carries `dir="ltr"` **and** `lang="en"`, i.e. is confined to the subordinate line.
      *And it caught the site `tsc` could not see* — see 5.9. Contradictor C predicted this test would stay green through 5.9; it would have, had the site been converted. Instead it went red and found the omission.
- [x] 10.3 `vi.mock("@/lib/api")` replaced the whole module with five functions, so the `statusOf` / `detailOf` the pages now import were **`undefined`** at test runtime — invisible until an error path actually ran. It now uses `importActual` and stubs only what does I/O: those helpers and `ApiError` are pure and must stay real. Exhaustive module mocking is a trap the moment the module gains a helper.
- [x] 10.4 Task 8.1's last two debts are cleared, each in the commit that unblocked it: the `/Open full Sourate page/` link query now reads `S.verse.openSurah` (unblocked by 6.2), and `conversations.test.ts` reads `S.chat.newConversation` (unblocked by 5.10). 79/79 — the count rose because the legacy-title mapping is new behaviour and got its own coverage rather than riding on an existing test.
