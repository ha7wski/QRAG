# Direct dependencies, pinned to the versions this project actually runs on.
# Install with uv:  ./scripts/setup.sh   (or: uv pip install -r requirements.txt)
# requirements.lock holds the fully-resolved transitive closure.

# ── Web / API ───────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.7
python-dotenv==1.0.0

# ── LLM providers ───────────────────────────────────────────
# anthropic is imported lazily in generation/llm_client.py and is only needed
# when LLM_PROVIDER=anthropic; ollama is the default provider.
ollama==0.6.2
anthropic>=0.40

# ── Embeddings / reranking ──────────────────────────────────
# torch 2.4 has no cp313 wheel — do not re-pin it downward without also
# pinning an older Python.
sentence-transformers==5.6.0
torch==2.12.0
transformers==5.12.1

# ── Vector DB ───────────────────────────────────────────────
qdrant-client==1.18.0

# ── BM25 sparse index ───────────────────────────────────────
rank-bm25==0.2.2

# ── Arabic NLP ──────────────────────────────────────────────
# camel-tools is deliberately absent: ingestion/morphology.py is the legacy
# stemmer, kept but unused (gated behind QAC_STEMMER_FALLBACK=0). Roots come
# from the QAC corpus. Install it only if you turn that fallback on:
#   uv pip install "camel-tools==1.5.4"
pyarabic==0.6.15
tashaphyne==0.3.6
langdetect==1.0.9

# ── Utils ───────────────────────────────────────────────────
httpx==0.28.1
tqdm==4.67.1

# Dropped (zero imports in the codebase): langchain, langchain-community,
# structlog, aiofiles.
